# 计划书模板

## 文件说明
- 功能说明：提供计划书最小结构，保证目标明确、边界清楚、验收可执行。
- 使用示例：新建 `ARCHITECTURE/plan/*.md` 时，可直接复制本模板并替换占位内容。

## 文档信息
- 目标 `spec`：[`spec/...`](../../spec/...)
- 目标 `API`：`<API 列表或文件路径>`
- 目标 `test`：[`test/...`](../../test/...)
- 目标 `验收资产`：`pytest ...` / `python ...` / 当前必过 `expectation` / 历史或本地只读 `expectation` 来源说明
- 目标 `功能实现`：[`path/to/impl`](../../path/to/impl)

## 计划级任务
- 流转：`execute -> review -> 架构复核 / 终验 -> merge/归档`
- 失败接续：`review` 不通过回 `execute`；架构复核 / 终验不通过也回 `execute`。

| 计划任务 | 任务类型 | worktree | 记录文件 |
| --- | --- | --- | --- |
| `<topic>` | `execute` | `<worktree>` | `<record.md>` |

## 评审摘要
- 评审结论：`<通过 / 最小需改项 / 不通过>`
- 评审人：`<name-a>`、`<name-b>`
- 结论摘要：`<一句话>`
- 当前状态：`<讨论中 / 待用户确认 / 可下发>`

## 多人讨论记录
- 讨论对象 1：`<对象 + 时间/记录路径 + 问题 + 观点 + 证据 + 分歧 + 采纳结论 + 遗留项>`
- 讨论对象 2：`<对象 + 时间/记录路径 + 问题 + 观点 + 证据 + 分歧 + 采纳结论 + 遗留项>`
- 讨论对象 3：`<对象 + 时间/记录路径 + 问题 + 观点 + 证据 + 分歧 + 采纳结论 + 遗留项>`

## 终验 / 复验 / 修复复核记录
- 结论人：`<name>`
- 结论：`<通过 / 不通过 / 复验通过 / 修复复核通过>`
- 验证基线：`<worktree / branch / commit>`
- 执行目录：`<path>`
- 同步结果：`<fetch / merge-base / 对齐结果；冲突或覆盖风险写阻塞>`
- 合同验收摘要：`<命令 + 结果>`
- 最小阻断项或通过摘要：`<一句话>`

## 计划目标
- `<目标 1>`
- `<目标 2>`
- `<目标 3>`

## 当前基线
- 当前公开合同：`<现状>`
- 当前公开 API：`<现状>`
- 当前实现入口：`<现状>`
- 当前测试与验收资产：`<现状>`
- 当前缺口：`<现状>`

## 方案比较与选型
- 不采用方案：`<方案>`
- 原因：`<原因>`
- 采用方案：`<方案>`
- 最小公开接口：`<接口>`

## 公开 API 设计
- 用户确认来源：`<用户明确同意的消息 / 记录路径；若未确认，写待用户确认>`
- `api_name(arg1: Type1, arg2: Type2) -> ReturnType`
- `class ApiClass(arg1: Type1)`
- `ApiClass.run(arg1: Type1, arg2: Type2) -> ReturnType`

## 完成态定义
- `<完成态 1>`
- `<完成态 2>`
- `<完成态 3>`

## 验收设计
- pytest / 脚本：`<命令>`
- 当前必过 expectation 合同验收：`<命令或不适用原因>`
- 历史 / 本地只读 expectation 来源说明：`<命令 / 路径 / 不适用原因；不得作为当前必跑项>`
- 锁定结果：`<输出 / 片段 / 行为>`
- Diff 反推要求：`执行与审查按实际 diff 补测试；expectation 单列为合同验收。`

## 计划内小任务

### S1. `<小任务标题>`
- 为什么做：`<当前缺口或质量目标>`
- 做什么：`<可执行动作 + 对象>`
- 不做什么：`<本卡明确排除项>`
- 怎么验收：`<diff 反推 pytest / script / 文本核对；写命令、核对点和预期结果>`
- 卡住问谁：`<具体角色或人名 + 触发条件；流程状态问管理员，公开 API / expectation 授权问用户，架构验收问架构师>`
- 上下文摘要：`<为什么需要做>`
- 小任务目标：`<可执行动作 + 对象 + 验收结果>`
- 非目标：`<本小任务不处理什么>`
- 模块范围：`<目录或子系统>`
- 禁止修改面：`<expectation/、计划书、标准文档、immutable 文件等；无额外禁止面时写无>`
- 合同真源：`<spec / pytest / 当前必过 expectation / 历史只读来源 / 当前实现的优先级>`
- 最小功能闭环：`<最小交付行为>`
- 执行步骤：
  1. `<第一步具体动作>`
  2. `<第二步具体动作>`
  3. `<第三步具体动作>`
- 验收必过项目：`<pytest / script；当前必过 expectation 合同验收单列；历史只读来源不得写成必过项>`
- 记录要求：`<任务记录应写清的内容>`

## 计划自检与返工口径
- 自检：`<公开 API、讨论记录、小任务短口径、验收资产、禁止修改面、expectation 权限、可读性和可维护性>`
- 返工口径：`只要仍有能提升质量、可读性、可维护性、测试有效性或验收可信度的可执行项，就回到计划修订或 execute 返工。`

## 待确认项
- 问题：`<问题>`
- 可选项：`<选项>`
- 推荐项：`<推荐项>`
- 影响：`<影响范围>`

## 用户确认与协同约束
- 用户确认来源：`<消息 / 记录路径>`
- 讨论对象 1：`<对象 + 结论 + 记录位置>`
- 讨论对象 2：`<对象 + 结论 + 记录位置>`
- 讨论对象 3：`<对象 + 结论 + 记录位置>`
