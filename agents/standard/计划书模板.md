# 计划书模板

## 文件说明
- 功能说明：提供本仓库计划书的最小模板与阶段正文模板，供架构师直接复制后填写。
- 使用示例：
  - 新建计划书时，先复制“最小模板”。
  - 填写单个阶段时，先复制“阶段正文模板”。
  - 标准说明见 [`agents/standard/计划书标准.md`](计划书标准.md)。

## 最小模板

~~~~markdown
# <topic>_green_plan.md

## 文档信息

- 创建者：`<name>`
- 最后一次更改：`<name>`
- 目标 `spec`：[`spec/...`](../../spec/...)
- 目标 `API`：[`path/to/api`](../../path/to/api)
- 目标 `test`：[`test/...`](../../test/...)
- 目标 `验收资产`：[`expectation/...`](../../expectation/...) / `pytest ...` / `python ...`
- 目标 `功能实现`：[`path/to/impl`](../../path/to/impl)

## 任务清单

| 任务 | 前置任务 | worktree | 记录文件 |
| --- | --- | --- | --- |
| S1 | 无 | `<worktree>` | `<record.md>` |

## 评审摘要

- 评审结论：`<通过 / 最小需改项 / 不通过>`
- 评审人：`<name-a>`、`<name-b>`
- 结论摘要：`<一句话摘要>`

## 计划目标

- `<目标 1>`
- `<目标 2>`
- `<目标 3>`

## 当前基线

- 当前公开合同：`<现状>`
- 当前公开 API：`<现状>`
- 当前实现入口：`<现状>`
- 当前测试与验收资产：`<现状>`
- 当前缺口或失败点：`<现状>`

## 方案比较与选型

- 不采用方案：`<方案>`
- 不采用原因：`<原因>`
- 采用方案：`<方案>`
- 最小公开接口：`<接口>`

## 公开 API 设计

- 公开入口：`<fn/class/tool>`
- 参数顺序：`<arg0, arg1, ...>`
- 参数类型：`<type list>`
- 返回值：`<return type>`

~~~python
<最小调用示例>
~~~

## 完成态定义

- `<完成态 1>`
- `<完成态 2>`
- `<完成态 3>`

## 验收设计

- 验收资产：[`expectation/...`](../../expectation/...) / `pytest ...` / `python ...`
- 输入样例：`<input>`
- 锁定输出：`<output>`
- 必过命令：`<command>`

## 阶段拆分

### S1：<阶段标题>

#### 阶段目标

- `<唯一直接产出>`

#### 目标 spec / API

- `spec/...`
- `公开 API：<函数/类/工具入口>`

#### 可改文件

- `<path-a>`
- `<path-b>`

#### 预期示例代码

~~~python
<最小输入样例>
~~~

#### 预期输出

~~~text
<最小可判断输出>
~~~

#### 目标验收资产

- `<验收资产路径 / 命令 / 补建要求>`
- `<该验收资产要锁定的行为>`

#### 验收必过项目

- `<命令 / expectation 路径 / 测试文件>`
- `<命令 / expectation 路径 / 测试文件>`

#### 任务新建建议

- `任务类型：<spec|build|review|merge|refactor|other>`
- `任务目标：<一句话>`
- `记录文件：<record.md>`

## 待确认项

- 问题：`<问题>`
- 可选项：`<选项>`
- 差异：`<差异>`
- 推荐项：`<推荐项>`

## 参考资料

- `<资料与借鉴点>`
~~~~

## 阶段正文模板

~~~~markdown
### S<编号>：<阶段标题>

#### 阶段目标

- <这一阶段唯一直接产出是什么>

#### 目标 spec / API

- `spec/...`
- `公开 API：<函数/类/工具入口>`

#### 可改文件

- `<path-a>`
- `<path-b>`

#### 预期示例代码

~~~python
<最小输入样例>
~~~

#### 预期输出

~~~text
<最小可判断输出>
~~~

#### 目标验收资产

- `<验收资产路径 / 命令 / 补建要求>`
- `<该验收资产要锁定的行为>`

#### 验收必过项目

- `<命令 / expectation 路径 / 测试文件>`
- `<命令 / expectation 路径 / 测试文件>`

#### 任务新建建议

- `任务类型：<spec|build|review|merge|refactor|other>`
- `任务目标：<一句话>`
- `记录文件：<record.md>`
~~~~

## 使用提醒

- 先按 [`agents/standard/计划书标准.md`](计划书标准.md) 补齐当前基线，再套模板。
- 模板只负责结构，不代替方案比较、边界判断与待确认项分析。
- 若阶段过大，先拆阶段，不要把多个主题塞进同一段模板。
- 写计划书时只写计划，不在计划文件里直接混入实现补丁、测试补丁或功能代码。
- 互评需要直接批注时，可用 `CMT: ... ENDCMT` 标出问题；收口后删除批注，并把结论写进评审摘要。
- 写完后按标准里的自检清单复核，至少确认自己不用照抄正文，也能复述目标、边界、公开 API、最小样例和验收资产。
