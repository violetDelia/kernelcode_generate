# 测试文件约定

- 功能说明：定义 pytest 基础配置与测试标记约定，明确基础设施测试与产品测试的分离方式。
- 使用示例：`pytest -m "not infra"`；`pytest -m infra`
- 文档信息：
  - 创建者：睡觉小分队
  - 最后一次更改：jcc你莫辜负
  - spec：[`agents/standard/测试文件约定.md`](../../agents/standard/测试文件约定.md)
  - test：[`test/codex-multi-agents`](../../test/codex-multi-agents)；[`test/script`](../../test/script)
  - 功能实现：[`pytest.ini`](../../pytest.ini)
- `test` 为测试目录。
- 测试统一使用 `pytest` 框架。
- pytest 配置统一写入 `pytest.ini` 的 `[pytest]`，必须包含 `infra` marker 声明。
- `test/codex-multi-agents` 与 `test/script` 的测试需标记 `infra`；其他测试默认不标记。
- `infra` marker 表示脚本、工具链、基础设施相关测试，产品测试需用 `-m "not infra"` 排除。
- 每份测试文件说明都要写明当前覆盖率信息和覆盖率命令；这里的覆盖率指对应功能实现文件或实现模块的覆盖率。
- 覆盖率命令必须明确指向对应功能实现文件或实现模块，而不是测试文件本身。
- 覆盖率命令应直接给出可执行示例，例如：`pytest --cov=package.module --cov-report=term-missing test/xxx/test_xxx.py`；其中 `--cov` 指向功能实现，测试文件仅作为执行入口。
- 若覆盖率未达标，需要申请补充测试；必要时先补充对应 `spec` 中的测试清单，再补测试实现。
- 每个测试函数都需添加注释，且注释中不写覆盖率信息。注释至少包含以下内容：
  - 创建者
  - 最后一次更改
  - 最近一次运行测试时间
  - 最近一次运行成功时间
  - 测试目的
  - 对应功能实现文件路径
  - 对应 `spec` 文件路径
  - 示例：[`test/codex-multi-agents/test_codex-multi-agents-list.py`](../../test/codex-multi-agents/test_codex-multi-agents-list.py)

## 改动文件到测试入口默认映射

- 本表用于执行人与审查人按实际 diff 反推测试；计划书或任务树列出的命令只是最低集合，不能替代表中对应入口。
- 本表只列 `pytest`、测试脚本或可作为测试执行的本地脚本；`expectation` 是合同验收资产，不属于 `Diff 反推测试`。
- 若文件注释、`spec/test/功能实现` 链接或相邻测试给出了更精确入口，优先执行更精确入口；本表作为缺省补充。

| 改动路径 | 默认测试入口 |
| --- | --- |
| `kernel_gen/dsl/gen_kernel.py`、`kernel_gen/dsl/gen_kernel/**` | `pytest -q test/dsl/test_gen_kernel.py` |
| `kernel_gen/dsl/emit_c.py`、`kernel_gen/dsl/gen_kernel/emit_c/**`、`kernel_gen/dsl/gen_kernel/dialect/**` | `pytest -q test/dsl/test_emit_c.py`；涉及 ircheck 文本时补跑 `pytest -q test/tools/test_ircheck_runner.py test/tools/test_ircheck_cli.py` |
| `kernel_gen/dsl/mlir_gen/**`、`kernel_gen/dsl/ast/**` | `pytest -q test/dsl/test_mlir_gen.py test/dsl/test_emit_mlir.py test/dsl/test_ast_visitor.py` |
| `kernel_gen/dialect/dma.py` | `pytest -q test/dialect/test_dma_dialect.py` |
| `kernel_gen/dialect/nn.py` | `pytest -q test/dialect/test_nn_dialect.py` |
| `kernel_gen/dialect/kernel.py` | `pytest -q test/dialect/test_kernel_dialect.py`；涉及 lowering/codegen 消费时补跑对应 `test/pass/**`、`test/dsl/**` |
| `kernel_gen/operation/**` | `pytest -q test/operation` |
| `kernel_gen/passes/**` | `pytest -q test/pass` 或对应 `test/pass/<pass>` |
| `kernel_gen/tools/ircheck.py` | `pytest -q test/tools/test_ircheck_runner.py test/tools/test_ircheck_cli.py` |
| `kernel_gen/tools/dsl_run.py` | `pytest -q test/tools/test_dsl_run.py` |
| `include/npu_demo/**` | `pytest -q test/include/npu_demo`；涉及生成代码时补跑 `pytest -q test/dsl/test_gen_kernel.py test/dsl/test_emit_c.py` |
| `agents/standard/**`、`agents/codex-multi-agents/agents/**` | `git diff --check -- <改动文件>`；必要时补跑相关脚本或 `rg` 核对新增规则是否被引用 |

## Expectation 合同验收补充

- `expectation` 可作为合同验收资产运行，但不计入 `Diff 反推测试`。
- 若任务直接修改 `expectation`，或改动涉及公开 IR / 源码 / 工具输出合同，应把对应 `python3 -m expectation...` 命令单列为“合同验收”，不能用它替代上表反推出的测试入口。

## 测试 ownership 约束（operation / dialect / dsl）

- 目标：明确测试职责边界，避免跨层测试重复或歧义。
- 约束表：

| 层级 | 关注对象 | 允许引用 | 不建议引用 | 代表目录 |
| --- | --- | --- | --- | --- |
| operation | operation helper 行为与输入输出约束 | `kernel_gen.operation.*`、`kernel_gen.symbol_variable.*` | `kernel_gen.dialect.*`、`kernel_gen.dsl.*` | `test/operation/**` |
| dialect | dialect op 校验与属性语义 | `kernel_gen.dialect.*`、`xdsl` | `kernel_gen.operation.*`、`kernel_gen.dsl.*` | `test/dialect/**` |
| dsl | DSL 解析、构建与发射链路 | `kernel_gen.dsl.*`、`kernel_gen.operation.*`（作为 helper 入口）、`kernel_gen.dialect.*`（IR 断言） | 将 operation/dialect 的单独语义当作测试目标 | `test/dsl/**` |

## 审计结果与最小调整原则

- 静态检索未发现 `test/operation/**` 直接引用 `kernel_gen.dialect.*` 或 `xdsl`。
- 静态检索未发现 `test/dialect/**` 直接引用 `kernel_gen.operation.*` 或 `kernel_gen.dsl.*`。
- `test/dsl/**` 允许同时引用 operation helper 与 dialect op，用于 DSL 解析与 IR 断言；这不属于跨层重复。
- 后续仅在出现明确重复或交叉引用证据时做最小调整，并优先移动或重命名测试而非改写实现。

## 结构说明与回归命令

- 结构说明：
  - pytest 配置：[`pytest.ini`](../../pytest.ini) 统一 `infra` marker；说明见本文件。
  - 公共错误模板：[`kernel_gen/core/contracts.py`](../../kernel_gen/core/contracts.py)；说明见 [`spec/core/contracts.md`](../../spec/core/contracts.md)。
  - dtype 常量：[`kernel_gen/symbol_variable/dtype_constants.py`](../../kernel_gen/symbol_variable/dtype_constants.py)；说明见 [`spec/symbol_variable/dtype_constants.md`](../../spec/symbol_variable/dtype_constants.md)。
  - DSL AST 入口：[`kernel_gen/dsl/ast.py`](../../kernel_gen/dsl/ast.py)；说明见 [`spec/dsl/ast.md`](../../spec/dsl/ast.md)。
- 回归命令：
  - `pytest -m "not infra"`
  - `pytest -m infra`
  - `pytest -q test/dsl`
  - `pytest -q test/operation`
  - `pytest -q test/dialect`
  - `rg "_ERROR_TEMPLATE\\s*=" kernel_gen`
  - `rg "_FLOAT_DTYPES\\s*=" kernel_gen`
  - `rg "_INT_DTYPES\\s*=" kernel_gen`
