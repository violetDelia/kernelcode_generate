# 计划书完成样板

## 文件说明
- 功能说明：给出一份最小合格计划书片段，便于互评时对照结构和粒度。
- 使用示例：计划书过于抽象时，按本样板补齐缺失字段。

## 文档信息
- 目标 `spec`：[`spec/demo/api.md`](../../spec/demo/api.md)
- 目标 `API`：`demo.run(input: str) -> str`
- 目标 `test`：[`test/demo/test_api.py`](../../test/demo/test_api.py)
- 目标 `验收资产`：`pytest -q test/demo/test_api.py`
- 目标 `功能实现`：[`demo/api.py`](../../demo/api.py)

## 计划级任务
| 计划任务 | 任务类型 | worktree | 记录文件 |
| --- | --- | --- | --- |
| `demo-plan` | `execute` | `wt-demo-plan` | `agents/codex-multi-agents/log/task_records/2026/16/20260415-demo-plan.md` |

## 迭代审阅记录

### Round 1：subagent strict review
- 审阅对象：`demo-subagent-a；记录=2026-05-16 talk`
- 输入标准包：`AGENTS.md + 当前角色 prompt + agents/standard/计划书标准.md + 计划草案全文 + 待确认项 + 禁止修改面 + pytest 验收命令`
- 严格通过口径：`公开 API、验收资产、禁止修改面和小任务卡均闭合；仍有可改进项则不通过。`
- 发现问题：`API 签名需要写参数和失败语义；测试需覆盖空输入失败路径。`
- 主线处理：`采纳，补齐 demo.run(input: str) -> str 与 S2 失败路径。`
- 状态：`已收口`

### Round 2：subagent strict review
- 审阅对象：`demo-subagent-b；记录=2026-05-16 talk`
- 输入标准包：`基于 Round 1 修订后的计划全文 + 上一轮问题和本轮收口摘要 + 其它标准包`
- 严格通过口径：`确认无未决 API、expectation 权限或任务卡缺口。`
- 发现问题：`无继续改进必要。`
- 主线处理：`无需修订。`
- 状态：`已收口`

### 守护最终检验
- 检验对象：`守护最好的爱莉希雅`
- 检验范围：`标准包、公开 API、expectation 权限、禁止修改面、验收命令和小任务卡`
- 必过门禁：`两轮 subagent 严格审阅已收口，用户确认项已落位。`
- 结论：`通过`
- 最小阻断项：`无`

## 计划目标
- 固定 `demo.run(input: str) -> str` 的公开调用方式。
- 补齐成功路径与空输入失败路径。
- 让执行人只看计划书即可完成实现和测试。

## 当前基线
- 当前公开合同：`spec/demo/api.md` 只有接口名，没有参数和失败路径。
- 当前公开 API：`demo.run`
- 当前实现入口：`demo/api.py`
- 当前测试与验收资产：`test/demo/test_api.py` 仅覆盖成功路径。
- 当前缺口：缺 API 签名、缺失败样例、缺验收命令。

## 方案比较与选型
- 不采用方案：只补测试。
- 原因：公开合同仍不完整。
- 采用方案：先补 `spec` 和 API 列表，再补实现与测试。
- 最小公开接口：`demo.run(input: str) -> str`

## 公开 API 设计
- 用户确认来源：`用户已确认 demo.run(input: str) -> str 为本轮公开 API`
- `demo.run(input: str) -> str`

## 完成态定义
- `spec/demo/api.md` 写清 API 签名、返回值和空输入失败语义。
- `demo/api.py` 实现公开行为。
- `pytest -q test/demo/test_api.py` 通过。

## 验收设计
- pytest：`pytest -q test/demo/test_api.py`
- expectation 合同验收：`不适用，本轮只锁定 Python API 行为。`
- 锁定结果：`run("hello") == "HELLO"`；空输入抛出公开错误。

## 计划内小任务

### S1. 补齐 demo.run 合同
- 为什么做：当前 `spec` 缺少签名和失败路径。
- 做什么：更新 `spec/demo/api.md` 的 API 列表，并写清 `demo.run(input: str) -> str`。
- 不做什么：不改实现。
- 怎么验收：`rg -n "demo.run|空输入" spec/demo/api.md` 命中 API 和失败路径。
- 卡住问谁：架构师；触发条件是 API 签名或失败语义无法从用户确认中判定。
- 上下文摘要：当前 `spec` 缺少签名和失败路径。
- 小任务目标：更新 `spec/demo/api.md` 的 API 列表，并写清 `demo.run(input: str) -> str`。
- 非目标：不改实现。
- 模块范围：`spec/demo/**`
- 禁止修改面：`expectation/`
- 合同真源：`spec/demo/api.md`
- 最小功能闭环：执行人能只读 `spec` 判断参数、返回值和失败语义。
- 执行步骤：
  1. 更新功能简介后的 API 列表。
  2. 补最小成功样例和空输入失败边界。
  3. 用 `rg -n "demo.run|空输入" spec/demo/api.md` 核对文本。
- 验收必过项目：`rg -n "demo.run|空输入" spec/demo/api.md`
- 记录要求：写清修改的合同字段和核对结果。

### S2. 实现并测试 demo.run
- 为什么做：合同已固定，需要实现公开行为并补测试。
- 做什么：实现 `demo.run(input: str) -> str`，并跑通 `pytest -q test/demo/test_api.py`。
- 不做什么：不新增其他公开 API。
- 怎么验收：`pytest -q test/demo/test_api.py` 通过，且成功与空输入失败断言有效。
- 卡住问谁：架构师；触发条件是合同与实现边界冲突。公开 API 变化必须转用户确认。
- 上下文摘要：合同已固定，需要实现公开行为并补测试。
- 小任务目标：实现 `demo.run(input: str) -> str`，并跑通 `pytest -q test/demo/test_api.py`。
- 非目标：不新增其他公开 API。
- 模块范围：`demo/**`、`test/demo/**`
- 禁止修改面：`expectation/`
- 合同真源：`spec/demo/api.md > test/demo/test_api.py`
- 最小功能闭环：成功路径和空输入失败路径都有断言。
- 执行步骤：
  1. 实现 `demo.run`。
  2. 补成功与失败测试。
  3. 运行 pytest 并记录 diff 反推自测。
- 验收必过项目：`pytest -q test/demo/test_api.py`
- 记录要求：写清实现入口、测试入口和结果。

## 计划自检与返工口径
- 自检：公开 API、迭代审阅记录、小任务短口径、验收资产和禁止修改面均已写清。
- 返工口径：若执行或审查发现实现可读性、可维护性、测试有效性或失败语义仍可改进，回到 execute 返工。

## 用户确认与协同约束
- 用户确认来源：`用户确认 demo.run(input: str) -> str`
- 用户已确认事项：`demo.run(input: str) -> str；记录=2026-05-16 user-confirm`
- 待用户确认项：`无`
- 迭代审阅记录：`Round 1 / Round 2 均在本计划正文`
- 守护最终检验：`通过；记录=本计划正文`
