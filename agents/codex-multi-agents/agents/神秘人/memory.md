
## 2026-04-01 Git 元数据写入异常（潜在阻塞）
- 现象：主仓 `/home/lfr/kernelcode_generate/.git` 被挂载为只读（mount 显示 `ro,errors=remount-ro`），在主仓执行 `git fetch` 报 `cannot open .git/FETCH_HEAD: Read-only file system`。
- 影响：可能导致后续所有 `commit/merge/fetch/push` 失败（包含各 worktree），一旦执行者反馈同类报错需优先排障/恢复可写后再推进合并链路。

## 2026-04-01 计划书（ARCHITECTURE/plan）执行硬约束
- 当 `ARCHITECTURE/plan/*.md` 给出计划时，任务分发必须严格按任务块原文执行：不改写任务块；仅在 `READY_IF` 满足时放行；分发消息必须原样包含该任务块的“目标、边界、注意事项、依赖、可改文件、下游需要覆盖层、验证命令、验收标准”。
- 计划任务需提前全部建档到 `TODO.md`（仅建档不提前分发），并在任务描述中显式标注依赖；计划依赖统一按“前置链路已合并”解释，禁止以“已完成但未合并”作为放行条件。
- 计划进度更新为硬要求：计划内任一任务进入新阶段时必须立即更新计划文件顶部“进度”段落；其中“记录文件”列仅填写文件名（不得绝对路径），“worktree” 列仅填写仓库内短路径（不得绝对路径）。
- 管理员需持续推进直到该计划全部任务完成并合并，最终交给 `大闸蟹` 做架构师验收。

## 2026-04-01 分发与沟通硬约束更新
- 执行角色发起任何“询问/澄清/阻塞上报”时，必须使用 `codex-multi-agents-tmux.sh -talk`；非脚本消息一律视为无效，要求其按脚本格式重发后再处理（重发前不做任务状态流转）。

## 2026-04-01 角色与矩阵更新
- `小李飞刀` 暂停使用：禁止向其分发任何任务；如需恢复必须由用户明确指示。
- 同一任务链中，若某角色已承担 `spec/实现/审查` 之一，则后续禁止再给其分发该链路中的其他职责类型任务。

## 2026-03-27 worktree 与主动提问硬约束
- 分发前必须先确认任务 `worktree` 真实存在且可访问；若目录缺失，管理员先创建/修复 `worktree` 后再分发，禁止“只写路径不建目录”。
- 所有执行角色在发现 `worktree` 缺失、路径不可用、环境/依赖缺失、需求歧义或约束冲突时，必须立即向管理员发起提问，不得静默等待或自行假设。
- 合并任务 `-done` 后，原链路记录文件禁止继续追加；若仍需做 `origin/main` 同步确认，必须使用独立任务与独立记录文件，不回写已封板日志。
- 执行角色可查阅当前任务链记录文件与同 `worktree` 相关日志以补全上下文，并可向其他角色询问；要求同步管理员、注明任务 ID，并将询问结论回写到当前记录文件。

## 2026-03-27 候补与审查硬约束更新
- 用户明确：全能替补（`朽木露琪亚`、`jcc你莫辜负`）不得承担 `合并` 与“合并后同步确认 origin/main”任务；候补仅用于 `spec/实现/测试/审查（含复审）` 兜底。
- 用户明确：审查人员是项目质量命脉；后续 `审查/复审` 必须重点检查功能正确性、边界条件、异常路径与潜在软件漏洞，未完成漏洞排查不得判定通过。

## 2026-03-27 审查与复审合并
- 用户明确：`审查` 与 `复审` 视为同一类工作，不再作为两套独立职责管理。
- 调度执行：后续链路中的 `审查/复审` 统一走“审查池”分发与验收。

## 2026-03-27 专精优先分发策略
- 团队分工更新为“专精优先、全能兜底”：
  - 实现专精：`小李飞刀`、`金铲铲大作战`
  - spec 专精：`睡觉小分队`、`咯咯咯`
  - 审查专精：`提莫炖蘑菇`、`不要啊教练`
  - 全能替补：`朽木露琪亚`、`jcc你莫辜负`
- 分发优先级：先在对应专精池内分发；当专精池成员均不可用（busy/冲突）时，才允许分发给全能替补。
- 用户既有硬约束继续生效：`李白` 仅负责 `合并` 与“合并后同步确认 origin/main”，不进入上述非合并分发池。

## 2026-03-27 专职合并约束
- 用户明确约束：固定 1 人为“专职合并角色”，该角色不参与 `spec/实现/审查/复审`，仅处理 `合并`（含合并后必要的 `origin/main` 同步确认）任务。
- 当前专职合并人固定为 `李白`；后续非合并任务禁止分发给 `李白`。
- 若 `李白` 忙碌导致合并排队，默认排队等待，不临时切换他人，除非用户再次明确授权变更。
