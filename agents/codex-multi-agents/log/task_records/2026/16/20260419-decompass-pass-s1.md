时间：2026-04-19 21:16
经办人：小李飞刀
任务：T-20260419-11a3a058
任务目标：修复 decompass expectation，并将 decompass 的 spec / 实现 / 专属 test 迁移到非 lowing 的 decompass 目录；同步收口相关引用与验收入口。
改动：在 `wt-20260419-decompass-pass-s1/kernel_gen/passes/decompass.py` 落地迁移后的唯一实现入口，去掉旧 `DecomposeNnSoftmax*` 兼容别名，并把 softmax axis 合同改成“只接受非负合法轴，负轴与越界统一报 DecompassError: normalized axis out of range”；同步更新 `wt-20260419-decompass-pass-s1/kernel_gen/passes/__init__.py`、`wt-20260419-decompass-pass-s1/kernel_gen/passes/lowering/__init__.py`、`wt-20260419-decompass-pass-s1/kernel_gen/passes/registry.py`、`wt-20260419-decompass-pass-s1/kernel_gen/passes/pipeline/default_lowering.py` 的公开导入路径与注册入口。将专题 spec / 专属 pytest 迁到 `wt-20260419-decompass-pass-s1/spec/pass/decompass.md`、`wt-20260419-decompass-pass-s1/test/pass/decompass/test_softmax.py`，并删除旧 `spec/pass/lowering/decompass.md`、`test/pass/test_decompose_nn_softmax.py`、`kernel_gen/passes/lowering/decompass.py`。在主仓 expectation 侧新增 `expectation/pass/decompass/_shared.py`，把 `expectation/pass/decompass/softmax.py` 的打印与随机样本辅助改为专题本地 helper，不再依赖 `expectation.pass.lowing._shared` / `expectation.pass.lowing.nn_lowering._random_utils`；同时更新 `expectation/pass/pipeline/default_lowering.py`，把 CASE-3 对齐为 decompass 阶段拒绝 `axis=-1`，并把临时 target hardware 键名改到当前 `tlm1/2/3_memory_size` 口径。
验证：`python3 -m pytest -q test/pass/decompass/test_softmax.py` -> `6 passed`；`python3 -m pytest -q test/pass/test_pass_manager.py -k decompass` -> `1 passed, 17 deselected`；`python3 -m pytest -q test/pass/test_pipeline_default_lowering.py` -> `2 passed`；`python3 -m pytest -q test/pass/test_pass_registry.py` -> `18 passed`；在 `wt-20260419-decompass-pass-s1` 目录下执行 `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/home/lfr/kernelcode_generate python3 -m expectation.pass.decompass` -> 通过；在同一目录下执行 `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/home/lfr/kernelcode_generate python3 -m expectation.pass.pipeline.default_lowering` -> 通过；`rg -n "kernel_gen\\.passes\\.lowering\\.decompass|spec/pass/lowering/decompass|test/pass/test_decompose_nn_softmax|expectation\\.pass\\.lowing\\._shared|expectation\\.pass\\.lowing\\.nn_lowering\\._random_utils|DecomposeNnSoftmaxPass|DecomposeNnSoftmaxError" kernel_gen spec test`（worktree 内执行）-> 无输出；`rg -n "expectation\\.pass\\.lowing\\._shared|expectation\\.pass\\.lowing\\.nn_lowering\\._random_utils" expectation/pass/decompass expectation/pass/pipeline/default_lowering.py`（主仓执行）-> 无输出。
结论：当前 build 已完成，任务日志已写入对应任务 worktree 的记录文件；下一步建议进入 review，重点复核迁移后的公开导入路径、`axis=-1` 非法合同，以及 expectation 需以 worktree 为当前目录执行这一复现方式。

时间：2026-04-19 21:19 +0800
经办人：不要啊教练
任务：T-20260419-11a3a058
任务目标：审查 decompass 目录迁移、axis 非法合同与 expectation 入口收口结果
改动：完成本轮审查。问题列表：1）[P1] 文件/接口：[`ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`](../../../../../../ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md:134)、[`ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`](../../../../../../ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md:163)、[`expectation/pass/decompass/__main__.py`](../../../../../../expectation/pass/decompass/__main__.py:1)、[`expectation/pass/decompass/softmax.py`](../../../../../../expectation/pass/decompass/softmax.py:1)、[`expectation/pass/pipeline/default_lowering.py`](../../../../../../expectation/pass/pipeline/default_lowering.py:1)；现象：目录迁移本身和 `axis=-1` 非法合同在实现 / spec / pytest / 主仓 expectation 中已经对齐，相关命令也能通过；但当前任务 `worktree` 自身没有 `expectation/` 目录。按 build 记录原文在 `worktree` 内执行 `rg ... kernel_gen spec test expectation` 会直接报 `expectation: No such file or directory`，而 `python -m expectation.pass.decompass` / `python -m expectation.pass.pipeline.default_lowering` 实际读取的也是主仓 `/home/lfr/kernelcode_generate/expectation/...`，不是任务现场自己的资产。风险：当前 “expectation 入口收口” 结论依赖主仓现场，不能作为该 `worktree` 可独立复现、可独立合入的证据；同时本轮还混入了 expectation 侧改动，但这些改动不在任务 `worktree` 中，后续 merge 无法从当前任务现场直接带走。建议：先把 expectation 资产纳入任务现场，或由架构侧明确 expectation 变更与验收基线应如何分离后，再回 review。
验证：`python3 -m pytest -q test/pass/decompass/test_softmax.py` -> `6 passed`；`python3 -m pytest -q test/pass/test_pass_manager.py -k decompass` -> `1 passed, 17 deselected`；`python3 -m pytest -q test/pass/test_pipeline_default_lowering.py` -> `2 passed`；`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/home/lfr/kernelcode_generate python3 -m expectation.pass.decompass`（在 `workdir=/home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1` 下执行）-> `exit 0`；`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/home/lfr/kernelcode_generate python3 -m expectation.pass.pipeline.default_lowering`（同一 workdir）-> `exit 0`；`test -d expectation; echo worktree_expectation:$?`（在 `wt-20260419-decompass-pass-s1` 下执行）-> `worktree_expectation:1`；`rg -n "kernel_gen\\.passes\\.lowering\\.decompass|spec/pass/lowering/decompass|test/pass/test_decompose_nn_softmax|expectation\\.pass\\.lowing\\._shared|expectation\\.pass\\.lowing\\.nn_lowering\\._random_utils|DecomposeNnSoftmaxPass|DecomposeNnSoftmaxError" kernel_gen spec test expectation`（在 `wt-20260419-decompass-pass-s1` 下执行）-> `exit 2`，`expectation: No such file or directory`。漏洞排查结果：1）输入校验绕过：`axis=-1` 与越界 axis 现在都稳定报 `DecompassError: normalized axis out of range`，这部分行为无新问题。2）类型/形状绕过：softmax 结果类型不匹配时的固定错误短语仍有 pytest 覆盖。3）边界越界：默认 pipeline 顺序和 decompass 根路径导入已对齐；当前缺口在 expectation 资产基线不在任务现场。4）错误处理缺失：错误路径与正向分解链都可复现；问题不在实现，而在验收现场边界。5）状态污染：当前通过结果依赖主仓 expectation 目录，不是纯 `worktree` 自包含现场。6）资源释放问题：本轮不涉及新增资源释放问题。改进建议：未发现额外改进点。
结论：需修改。下一步建议创建 `build` 任务，任务目标为“对齐 decompass expectation 资产所在现场与验收命令，使 task worktree 可直接复现并承载本轮 expectation 收口结果”；若 expectation 侧改动应独立处理，请先由架构师明确边界。

时间：2026-04-19 21:24 +0800
经办人：朽木露琪亚
任务：T-20260419-11a3a058
任务目标：对齐 decompass expectation 资产所在现场与验收命令，使当前任务 `worktree` 可直接复现并承载本轮 expectation 收口结果。
改动：在当前 `worktree` 下补齐本地 expectation 现场，新增本地 `expectation/pass/decompass`、`expectation/pass/pipeline/default_lowering.py`、`expectation/utils/` 与 `expectation/utils/random/`，使 `python3 -m expectation.pass.decompass` 与 `python3 -m expectation.pass.pipeline.default_lowering` 可以在 `wt-20260419-decompass-pass-s1` 内直接用 `PYTHONPATH=.` 运行，不再借主仓 `/home/lfr/kernelcode_generate/expectation`；本轮未改 expectation 内容，只把主仓已收口的专题资产同步到任务现场，并保留现有实现 / spec / pytest 改动不变。
验证：`test -d expectation && find expectation -maxdepth 3 -type f | sort` -> 当前任务 `worktree` 已包含 `expectation/pass/decompass`、`expectation/pass/pipeline/default_lowering.py`、`expectation/utils/*`；`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -m expectation.pass.decompass` -> `exit 0`；`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -m expectation.pass.pipeline.default_lowering` -> `exit 0`；`python3 -m pytest -q test/pass/decompass/test_softmax.py` -> `6 passed`；`python3 -m pytest -q test/pass/test_pass_manager.py -k decompass` -> `1 passed, 17 deselected`；`python3 -m pytest -q test/pass/test_pipeline_default_lowering.py` -> `2 passed`；`rg -n "kernel_gen\\.passes\\.lowering\\.decompass|spec/pass/lowering/decompass|test/pass/test_decompose_nn_softmax|expectation\\.pass\\.lowing\\._shared|expectation\\.pass\\.lowing\\.nn_lowering\\._random_utils|DecomposeNnSoftmaxPass|DecomposeNnSoftmaxError" kernel_gen spec test expectation` -> `exit 1`，未命中旧路径/旧别名；`git check-ignore -v expectation/pass/decompass/__main__.py expectation/pass/pipeline/default_lowering.py expectation/utils/case_runner.py` -> 命中 `.gitignore:17:expectation`，说明本地 expectation 现场按仓库规则保持忽略，但已在任务 `worktree` 就位可供复现。
结论：当前 build 已完成，任务 `worktree` 现已自带 decompass expectation 现场，相关验收命令可直接在本地用 `PYTHONPATH=.` 复现；下一步建议进入 review，重点复核“本地 expectation 现场已补齐且不再借主仓路径”这一条是否满足收口要求。

时间：2026-04-19 21:26 +0800
经办人：不要啊教练
任务：T-20260419-11a3a058
任务目标：复核 decompass expectation 资产已补齐到 wt-20260419-decompass-pass-s1，本地 PYTHONPATH=. 验收命令可直接复现，且不再借主仓 expectation 路径
改动：完成本轮复审。问题列表：1）[P1] 文件/接口：[.gitignore](../../../../../../.gitignore:17)、[`agents/standard/expectation任务规则.md`](../../../../../../agents/standard/expectation任务规则.md:9)、[`agents/standard/expectation任务规则.md`](../../../../../../agents/standard/expectation任务规则.md:11)、[`agents/standard/expectation任务规则.md`](../../../../../../agents/standard/expectation任务规则.md:14)、[`expectation/pass/decompass/__main__.py`](../../../../../../expectation/pass/decompass/__main__.py:1)、[`expectation/pass/decompass/softmax.py`](../../../../../../expectation/pass/decompass/softmax.py:1)、[`expectation/pass/decompass/_shared.py`](../../../../../../expectation/pass/decompass/_shared.py:1)、[`expectation/pass/pipeline/default_lowering.py`](../../../../../../expectation/pass/pipeline/default_lowering.py:1)；现象：当前 `PYTHONPATH=.` 口径已经成立，我复测时实际导入到的是当前 `worktree` 下的 `expectation.pass.decompass.__main__`、`expectation.pass.pipeline.default_lowering`、`expectation.pass.decompass._shared` 与 `expectation.utils.case_runner`，不再借主仓路径；但这批 expectation 文件仍被 [.gitignore](../../../../../../.gitignore:17) 整体忽略，且 expectation 规则明确写着“非架构师不得直接修改仓库中的 expectation 文件”“不得把 expectation 改动纳入正常合并内容”“合并时若发现非架构师提交的 expectation 改动，应拒绝合并并回报管理员与架构师”。本轮 expectation 现场虽然补到了 `worktree`，但仍不在可正常合入范围内。风险：当前本地复现能力只是当前现场有效，一旦进入后续合入阶段，这批被忽略的 expectation 资产无法作为正常变更进入主线；同时 expectation 所有权也与仓库通用规则冲突。建议：先由架构师决定 expectation 资产应如何纳入正式链路，或改成不修改 expectation 本体也能完成验收的方案；在此之前不应把这批 expectation 文件当作已收口的可合入结果。
验证：`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -m expectation.pass.decompass` -> `exit 0`；`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -m expectation.pass.pipeline.default_lowering` -> `exit 0`；`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 - <<'PY' ... importlib.import_module(...) ... PY` -> `expectation.pass.decompass.__main__`、`expectation.pass.pipeline.default_lowering`、`expectation.pass.decompass._shared`、`expectation.utils.case_runner` 的 `__file__` 全部指向 `wt-20260419-decompass-pass-s1/expectation/...`；`git check-ignore -v expectation/pass/decompass/__main__.py expectation/pass/decompass/softmax.py expectation/pass/decompass/_shared.py expectation/pass/pipeline/default_lowering.py expectation/utils/case_runner.py` -> 均命中 [.gitignore](../../../../../../.gitignore:17)；`python3 -m pytest -q test/pass/decompass/test_softmax.py` -> `6 passed`；`python3 -m pytest -q test/pass/test_pass_manager.py -k decompass` -> `1 passed, 17 deselected`；`python3 -m pytest -q test/pass/test_pipeline_default_lowering.py` -> `2 passed`。漏洞排查结果：1）输入校验绕过：`axis=-1` 与越界 axis 仍稳定报 `DecompassError: normalized axis out of range`，行为侧无新问题。2）类型/形状绕过：softmax 结果类型不匹配的固定错误短语仍有 pytest 覆盖。3）边界越界：本地 expectation 现场已补齐并可直接运行，但其所有权与合入边界未收齐。4）错误处理缺失：decompass 与默认 pipeline expectation 命令均可直接复现，问题不在运行入口。5）状态污染：当前通过结果已不再借主仓 expectation 路径，但仍依赖本地忽略文件；这不是可直接带入后续合入的正式现场。6）资源释放问题：本轮不涉及新增资源问题。改进建议：未发现额外改进点。
结论：需修改。下一步建议创建 `spec` 任务，并明确由架构师处理 expectation 资产所有权与合入边界；若不希望 expectation 本体进入正式变更，则需把验收方式改成不依赖这批本地忽略文件。

时间：2026-04-19 21:36 +0800
经办人：睡觉小分队
任务：T-20260419-11a3a058
任务目标：收齐 decompass expectation 资产所有权与正常合入边界，并把当前任务链的验收口径改成不依赖本地 expectation 忽略副本。
改动：更新 `ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`，把“正式合入资产”与“架构侧合同参考”拆开，明确非架构师不得把 expectation 文件纳入正常合并内容，`worktree` 内同名 expectation 副本只可作为现场复现材料；同步更新 `wt-20260419-decompass-pass-s1/spec/pass/decompass.md` 与 `wt-20260419-decompass-pass-s1/spec/pass/pipeline/default_lowering.md`，把正式验收改为依赖 `pytest`、公开导入路径与顺序口径检查，不再把本地 expectation 副本写成必过前置，同时保留架构侧 runner 的补充核对命令。
验证：`rg -n '正式验收不依赖当前 task worktree 内被忽略的 expectation 副本|非架构师不得把 expectation 文件纳入正常合并内容|PYTHONPATH=<task_worktree>:<repo_root> python3 -m expectation.pass.decompass|当前任务链的正式验收只依赖 .*公开导入路径与 .*|当前任务链的正式验收不依赖 .*本地 expectation 副本' /home/lfr/kernelcode_generate/ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md /home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1/spec/pass/decompass.md /home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1/spec/pass/pipeline/default_lowering.md` -> 命中计划书与两个 spec 的新口径；`nl -ba /home/lfr/kernelcode_generate/ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md | sed -n '152,206p'` 与 `nl -ba /home/lfr/kernelcode_generate/ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md | sed -n '234,256p'` -> 正式合入资产、架构侧合同参考、验收命令与正常合入边界已分开；`nl -ba /home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1/spec/pass/decompass.md | sed -n '33,138p'` 与 `nl -ba /home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1/spec/pass/pipeline/default_lowering.md | sed -n '95,125p'` -> decompass 与 default_lowering 的正式验收口径均已改为不依赖本地 expectation 副本；未执行 pytest，原因：当前阶段只调整 plan/spec 文本，不改实现、测试与 expectation 本体。
结论：当前 spec 已完成，任务日志已写入对应任务 worktree 的记录文件；下一步建议进入 review，复核计划书与 spec 已将 expectation 资产所有权、正常合入边界与补充核对方式写清。

时间：2026-04-19 21:38 +0800
经办人：不要啊教练
任务：T-20260419-11a3a058
任务目标：复核 decompass 计划书与 spec 已收齐 expectation 资产所有权、正常合入边界与补充核对口径，确认正式验收不再依赖本地 expectation 忽略副本
改动：完成本轮复审。问题列表：1）[P1] 文件/接口：[`ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`](../../../../../../ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md:178)、[`ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`](../../../../../../ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md:255)、[`spec/pass/decompass.md`](../../../../../../spec/pass/decompass.md:138)；现象：计划书和专题 `spec` 已经把 expectation 资产所有权、正常合入边界和补充核对方式拆开了，正式验收也不再依赖本地 `expectation` 副本；但正式验收里的最后一条 `rg -n "kernel_gen\\.passes\\.lowering\\.decompass|spec/pass/lowering/decompass|test/pass/test_decompose_nn_softmax" kernel_gen spec test` 仍会命中 [`spec/pass/decompass.md`](../../../../../../spec/pass/decompass.md:138) 这一行自己写下的命令示例。也就是说，当前正式验收口径虽然不再依赖本地 expectation 副本，但还不能按文档原文得到“空结果即通过”的结论。风险：下游照着 plan/spec 原文复验时，会把这条自命中误判成旧路径残留，正式验收仍然不够自解释。建议：把这条 `rg` 再收紧一层，例如排除当前 `spec` 自身的命令示例，或把检查范围改成只扫实现/测试文件后再回 review。
验证：`python3 -m pytest -q test/pass/decompass/test_softmax.py` -> `6 passed`；`python3 -m pytest -q test/pass/test_pass_manager.py -k decompass` -> `1 passed, 17 deselected`；`python3 -m pytest -q test/pass/test_pipeline_default_lowering.py` -> `2 passed`；`rg -n "kernel_gen\\.passes\\.lowering\\.decompass|spec/pass/lowering/decompass|test/pass/test_decompose_nn_softmax" kernel_gen spec test` -> 命中 [`spec/pass/decompass.md`](../../../../../../spec/pass/decompass.md:138) 的命令示例文本；文本核对 [`ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`](../../../../../../ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md:174)、[`ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`](../../../../../../ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md:179)、[`ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`](../../../../../../ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md:183)、[`ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`](../../../../../../ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md:184)、[`spec/pass/decompass.md`](../../../../../../spec/pass/decompass.md:40)、[`spec/pass/pipeline/default_lowering.md`](../../../../../../spec/pass/pipeline/default_lowering.md:104)、[`spec/pass/pipeline/default_lowering.md`](../../../../../../spec/pass/pipeline/default_lowering.md:118) 可见 expectation 所有权、正式验收边界与补充核对口径已经收齐。漏洞排查结果：1）输入校验绕过：`axis=-1` 与越界 axis 仍稳定报 `DecompassError: normalized axis out of range`，行为侧无新问题。2）类型/形状绕过：softmax 结果类型不匹配的固定错误短语仍有 pytest 覆盖。3）边界越界：expectation 所有权与正式合入边界已写清，当前缺口只在旧路径 `rg` 口径自命中。4）错误处理缺失：正式 pytest 与补充核对 runner 都能复现，问题不在运行入口。5）状态污染：`PYTHONPATH=.` 时 expectation 模块已从当前 `worktree` 加载，但这已被降级为补充核对，不再作为正式验收前置。6）资源释放问题：本轮不涉及新增资源问题。改进建议：未发现额外改进点。
结论：需修改。下一步建议创建 `spec` 任务，任务目标为“收紧 decompass 正式验收中的旧路径 `rg` 口径，避免命中 spec 自身命令示例文本”。

时间：2026-04-19 21:42 +0800
经办人：咯咯咯
任务：T-20260419-11a3a058
任务目标：收紧 decompass 正式验收中的旧路径 `rg` 口径，避免命中专题 `spec` 自身的命令示例文本。
改动：更新 `ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md` 与 `wt-20260419-decompass-pass-s1/spec/pass/decompass.md`，把正式验收中的旧路径检查命令统一改为 `rg -n --glob '!spec/pass/decompass.md' "kernel_gen\\.passes\\.lowering\\.decompass|spec/pass/lowering/decompass|test/pass/test_decompose_nn_softmax" kernel_gen spec test`，保留对实现、测试与其它 spec 文本的旧路径扫描，同时排除当前专题 `spec` 自己的命令示例行；同步把两份文档的“最后一次更改”更新为 `咯咯咯`。
验证：`nl -ba /home/lfr/kernelcode_generate/ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md | sed -n '174,180p'` 与 `nl -ba /home/lfr/kernelcode_generate/ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md | sed -n '252,256p'` -> 两处正式验收命令已同步为排除 `spec/pass/decompass.md` 的新写法；`sed -n '1,18p' /home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1/spec/pass/decompass.md` -> 文档元信息已更新；在 `wt-20260419-decompass-pass-s1` 下执行 `rg -n --glob '!spec/pass/decompass.md' "kernel_gen\\.passes\\.lowering\\.decompass|spec/pass/lowering/decompass|test/pass/test_decompose_nn_softmax" kernel_gen spec test` -> 无输出，退出码 `1`；未执行 `pytest`，原因：当前轮次只调整 plan/spec 文本，不改实现与测试。
结论：当前 spec 已完成，任务日志已写入对应任务 worktree 的记录文件；下一步建议进入 review，复核正式验收命令已不再命中专题 spec 自身示例文本。

时间：2026-04-19 21:44 +0800
经办人：不要啊教练
任务：T-20260419-11a3a058
任务目标：复核 decompass 正式验收中的旧路径 rg 口径已收紧，不再命中 spec 自身命令示例文本
改动：完成本轮复审，未发现新的审查问题。当前 [`ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`](../../../../../../ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md:178)、[`ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md`](../../../../../../ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md:255) 与 [`spec/pass/decompass.md`](../../../../../../spec/pass/decompass.md:138) 已统一改为 `rg -n --glob '!spec/pass/decompass.md' ... kernel_gen spec test`，正式验收中的旧路径扫描不再命中专题 `spec` 自身示例文本；其余 expectation 所有权、正式合入边界与补充核对口径也保持前一轮已收齐状态。
验证：`python3 -m pytest -q test/pass/decompass/test_softmax.py` -> `6 passed`；`python3 -m pytest -q test/pass/test_pass_manager.py -k decompass` -> `1 passed, 17 deselected`；`python3 -m pytest -q test/pass/test_pipeline_default_lowering.py` -> `2 passed`；`rg -n --glob '!spec/pass/decompass.md' "kernel_gen\\.passes\\.lowering\\.decompass|spec/pass/lowering/decompass|test/pass/test_decompose_nn_softmax" kernel_gen spec test`（在 `wt-20260419-decompass-pass-s1` 下执行）-> 无输出，`exit 1`；`rg -n --fixed-strings -- "--glob '!spec/pass/decompass.md'" /home/lfr/kernelcode_generate/ARCHITECTURE/plan/decompass_pass_rehome_green_plan.md /home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1/spec/pass/decompass.md` -> 命中计划书两处与专题 `spec` 一处的新命令文本。漏洞排查结果：1）输入校验绕过：`axis=-1` 与越界 axis 的非法合同未受本轮文本调整影响。2）类型/形状绕过：softmax 结果类型不匹配的错误路径仍由既有 pytest 覆盖。3）边界越界：旧路径扫描已排除专题 `spec` 自命中，当前正式验收口径与实测一致。4）错误处理缺失：三条正式 pytest 与旧路径扫描均可按文档原文复现。5）状态污染：当前正式验收仍不依赖本地 `expectation` 副本，和前一轮边界说明一致。6）资源释放问题：本轮未涉及新增资源问题。改进建议：未发现额外改进点。
结论：通过。下一步建议创建 `merge` 任务，按正式验收通过路径继续推进。

时间：2026-04-20 02:22 +0800
经办人：李白
任务：T-20260419-11a3a058
任务目标：按正式验收通过路径开始合并当前 worktree 的 decompass 目录迁移改动。
改动：确认当前 `worktree` 仍保留待合入写集：`kernel_gen/passes/__init__.py`、`kernel_gen/passes/lowering/__init__.py`、`kernel_gen/passes/lowering/decompass.py` 删除、`kernel_gen/passes/pipeline/default_lowering.py`、`kernel_gen/passes/registry.py`、`spec/dialect/nn.md`、`spec/pass/lowering/decompass.md` 删除、`spec/pass/pipeline/default_lowering.md`、`test/pass/test_decompose_nn_softmax.py` 删除、`test/pass/test_pass_manager.py`、`test/pass/test_pipeline_default_lowering.py`、`kernel_gen/passes/decompass.py`、`spec/pass/decompass.md`、`test/pass/decompass/test_softmax.py` 与当前记录文件；当前 base 仍落后于最新 `origin/main`，需要先把本轮合并结果纳入最新主线再推送。
验证：`git -C /home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1 status --short --branch --untracked-files=all` -> 仅命中上述业务文件与记录文件；`git -C /home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1 merge-base --is-ancestor HEAD origin/main; echo $?` -> `0`，`git -C /home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1 merge-base --is-ancestor origin/main HEAD; echo $?` -> `1`，确认当前 worktree 还在旧基线；`git -C /home/lfr/kernelcode_generate/wt-20260419-decompass-pass-s1 diff --stat` -> 命中本轮待合并的 11 个业务文件与记录文件。
结论：开始合并，下一步先把当前写集提交并重放到最新 `origin/main`，再执行推送与 `-done`。
