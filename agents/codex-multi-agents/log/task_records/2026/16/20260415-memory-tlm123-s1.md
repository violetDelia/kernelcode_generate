时间：2026-04-15 02:31 +0800
经办人：咯咯咯
任务：T-20260415-3c55bb63
任务目标：收口 MemorySpace / BarrierVisibility / BarrierScope 与 npu_demo target 的 tlm1/2/3 容量字段合同（S1 spec）
改动：
- 更新 `spec/include/api/Memory.md`：公开 `MemorySpace` 从 `GM/SM/LM/TSM/TLM` 调整为 `GM/SM/LM/TSM/TLM1/TLM2/TLM3`，并明确 `MemorySpace::TLM` 不再作为公开输入。
- 更新 `spec/include/api/Arch.md`：新增 `BarrierVisibility` 公开接口，明确 `BarrierVisibility::TLM` 为聚合可见域；`BarrierScope` 公开成员调整为 `BLOCK/THREAD/SUBTHREAD/GLOBAL`；`barrier(visibility, scope)` 入参改为 `BarrierVisibility` 列表。
- 更新 `spec/symbol_variable/memory.md`：同步 `MemorySpace` 七个公开成员，补充 `TLM1/TLM2/TLM3` 用法与旧 `TLM` 拒绝口径，并补充对应测试映射（待补）。
- 更新 `spec/target/registry.md`：`npu_demo`/`txt/json` 示例与字段映射由 `tlm_memory_size` 调整为 `tlm1_memory_size/tlm2_memory_size/tlm3_memory_size`，并同步联动约束与测试清单。
验证：
- `git -C /home/lfr/kernelcode_generate/wt-20260415-memory-tlm123-s1 diff --name-only`：确认改动仅包含 4 个目标 spec 文件。
- `rg -n "MemorySpace::TLM|TLM1|TLM2|TLM3|BarrierVisibility|BarrierScope" spec/include/api/Memory.md spec/include/api/Arch.md spec/symbol_variable/memory.md`：确认公开接口与枚举口径已同步。
- `rg -n "tlm_memory_size|tlm1_memory_size|tlm2_memory_size|tlm3_memory_size" spec/target/registry.md`：确认 target 容量字段已替换为三块 `tlm` 字段。
- 未执行测试命令，原因：本轮为 spec 文档任务，仅收口公开合同，不涉及实现与测试文件修改。
结论：S1 spec 口径已完成收口，满足后续 build 阶段实现 `TLM1/TLM2/TLM3` 与 `BarrierVisibility/BarrierScope` 的输入约束与容量字段对齐需求。

时间：2026-04-15 04:59 +0800
经办人：金铲铲大作战
任务：T-20260415-3c55bb63
任务目标：按 S1 spec 完成 `MemorySpace` / `BarrierVisibility` / `BarrierScope` 与 `npu_demo` tlm1/2/3 容量字段的实现和测试对齐。
改动：
- 更新 `kernel_gen/symbol_variable/memory.py`：`MemorySpace` 从 `TLM` 拆分为 `TLM1/TLM2/TLM3`，同步注释说明。
- 更新 `kernel_gen/target/registry.py`：target 硬件字段改为 `tlm1_memory_size/tlm2_memory_size/tlm3_memory_size`，并把内置 `cpu/npu_demo` 默认硬件值切到三块 `tlm` 容量。
- 更新 `include/api/Memory.h`：公开 `MemorySpace` 改为 `GM/SM/LM/TSM/TLM1/TLM2/TLM3`，模板简写常量同步到 `TLM1/TLM2/TLM3`。
- 更新 `include/api/Arch.h`：新增 `BarrierVisibility::{TSM,TLM}`，并把 `BarrierScope` 扩为 `BLOCK/THREAD/SUBTHREAD/GLOBAL`。
- 额外更新 `include/npu_demo/Arch.h`：由于 `test/include/api/test_arch.py` 会直接编译 `include/npu_demo/Arch.h`，必须同步把 `KernelContext::barrier(...)` 改为 `BarrierVisibility`，并把 `get_dynamic_memory<Space, T>()` 切到 `TLM1/TLM2/TLM3`，否则 API 编译回归无法通过。
- 额外更新 `kernel_gen/target/targets/cpu.txt`：将旧 `hw.tlm_memory_size=0` 改为 `hw.tlm1_memory_size=0`、`hw.tlm2_memory_size=0`、`hw.tlm3_memory_size=0`，用于对齐默认 target 目录加载测试。
- 更新测试：`test/include/api/test_memory.py`、`test/include/api/test_arch.py`、`test/target/test_target_registry.py`、`test/symbol_variable/test_memory.py`、`test/symbol_variable/test_memory_operation.py`，补齐 `TLM1/TLM2/TLM3`、`BarrierVisibility`、扩展 `BarrierScope` 与旧 `TLM` 拒绝路径覆盖。
验证：
- `pytest -q test/include/api/test_memory.py test/include/api/test_arch.py` -> `7 passed in 0.73s`，exit 0。
- `pytest -q test/target/test_target_registry.py` -> `19 passed in 0.04s`，exit 0。
- `pytest -q test/symbol_variable/test_memory.py test/symbol_variable/test_memory_operation.py` -> `22 passed in 0.19s`，exit 0。
- `git diff --name-only`：当前 worktree 还包含前序 S1 spec 留存改动 `spec/include/api/Arch.md`、`spec/include/api/Memory.md`、`spec/symbol_variable/memory.md`、`spec/target/registry.md`；本轮 build 未继续修改这些 spec 文件。
结论：
- 本轮 build 已完成，S1 所需公开空间、可见域、scope 与 `npu_demo`/target 三块 `tlm` 容量实现已对齐，指定 pytest 全部通过。
- 下一步建议进入 review，复核公开头文件、target 默认值与新增回归是否与 S1 spec 一致。

时间：2026-04-15 05:05 +0800
经办人：不要啊教练
任务：T-20260415-3c55bb63
任务目标：复核 S1 中 MemorySpace/BarrierVisibility/BarrierScope 与 npu_demo tlm1/2/3 容量字段实现及测试对齐结果。
改动：
- 审查结论：通过。
- 范围核对：`git diff --name-only` 显示改动集中在 `include/api/Arch.h`、`include/api/Memory.h`、`include/npu_demo/Arch.h`、`kernel_gen/symbol_variable/memory.py`、`kernel_gen/target/registry.py`、`kernel_gen/target/targets/cpu.txt`、S1 对应 spec 与测试文件；与任务目标匹配。
- 一致性核对：
  - `MemorySpace` 已由公开 `TLM` 拆分为 `TLM1/TLM2/TLM3`（API、symbol_variable、target 硬件字段、测试同步）。
  - `BarrierVisibility::{TSM,TLM}` 与 `BarrierScope::{BLOCK,THREAD,SUBTHREAD,GLOBAL}` 已在 API 头与测试中同步。
  - `npu_demo::KernelContext` 已切换到 `BarrierVisibility` 入参，并提供 `TLM1/TLM2/TLM3` 三块动态内存入口与容量映射。
  - target 默认硬件字段已切到 `tlm1_memory_size/tlm2_memory_size/tlm3_memory_size`，CPU/NPU 模板与 txt 载入测试对齐。
- 风险排查结果：
  - 输入校验绕过：`KernelContext::barrier` 对 visibility 组合与 scope 仍有显式拒绝逻辑，未见绕过。
  - 类型/形状绕过：`get_dynamic_memory<Space,T>()` 仅放行受支持空间模板，旧 `TLM` 在公开入口被拒绝。
  - 边界越界：本轮未引入新的索引访问路径；动态内存容量由固定硬件字段提供。
  - 错误处理缺失：新增负向用例覆盖旧 `MemorySpace::TLM` 编译失败路径。
  - 状态污染：本轮为枚举与配置字段扩展，未引入共享可变状态。
  - 资源释放问题：本轮无新增动态资源生命周期管理逻辑。
- 改进建议：未发现额外改进项。
验证：
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. pytest -q test/include/api/test_memory.py test/include/api/test_arch.py` -> `7 passed`，exit=0。
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. pytest -q test/target/test_target_registry.py` -> `19 passed`，exit=0。
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. pytest -q test/symbol_variable/test_memory.py test/symbol_variable/test_memory_operation.py` -> `22 passed`，exit=0。
- `git diff --name-only`：范围与 S1 目标一致，未见 expectation 文件改动。
结论：S1 build 结果与计划书口径一致，复测通过，可续接 merge。
