### 时间
- 2026-03-27 09:47:00 +0800

### 任务
- T-20260327-0d7a3b82

### 任务目标
- 完成 nn.fc（fully connected）spec 阶段：定义 bias 可选参数语义，补齐输入/权重/bias 约束、输出推导、错误路径与 spec->测试映射。

### 改动
- 更新 `spec/operation/nn.md`：
  - 在功能范围中新增 `fc`，明确 `fc(value, weight, bias=None)`。
  - 明确 `bias` 为可选参数；提供时要求 `bias.rank == 1` 且 `bias.shape[0] == out_features`。
  - 补齐 `value/weight/bias` 的类型、rank、shape、space、dtype 兼容约束与报错口径（`TypeError` / `ValueError`）。
  - 明确批维保留与输出 shape 推导规则，以及与现有 nn 算子链式兼容关系。
  - 在测试目标与功能映射中新增 `OP-FC-001`~`OP-FC-007`，覆盖无 bias/可选 bias/类型错误/rank 错误/特征维不匹配/bias 形状错误/space 错误。

### 结论
- spec 阶段已完成，满足任务要求。
- 本任务仅涉及规范文档，不涉及实现代码与测试执行。
- 建议下一步进入实现阶段：在 `kernel_gen/operation/nn.py` 与 `test/operation/test_operation_nn.py` 完成 `fc` 功能和 `OP-FC-001`~`OP-FC-007` 测试闭环。

### 时间
- 2026-03-27 09:39:01 +0800

### 任务
- T-20260327-400ba7e5

### 经办人
- 小李飞刀

### 任务目标
- 实现 nn.fc(value, weight, bias=None) 并补齐 OP-FC-001~OP-FC-007 测试闭环，落实 bias 可选、shape/dtype/space 校验与错误路径。

### 改动
- 更新 `kernel_gen/operation/nn.py`：新增 `fc` 实现，补齐类型、rank、特征维、bias 维度与 space 校验，按 add 规则决议 dtype，输出默认布局。
- 更新 `test/operation/test_operation_nn.py`：新增 `test_nn_fc_*` 覆盖 OP-FC-001~OP-FC-007，并补充 import。

### 验证
- pytest -q test/operation/test_operation_nn.py (exit=0)

### 结论
- 实现与测试已收敛，建议进入审查阶段。

时间: 2026-03-27 10:00:20 +0800
任务: T-20260327-dc33e774
任务目标: 复审 nn.fc 实现与 OP-FC-001~007 测试闭环，重点核对 bias 可选、shape/dtype/space 校验与错误路径。
检查:
- spec/operation/nn.md: fc 接口约束与错误规则完整，覆盖 bias 可选、shape/dtype/space 校验与输出布局。
- kernel_gen/operation/nn.py: fc 实现校验 Memory 类型、rank、feature 维匹配、space 一致性；bias 可选且 shape/space/dtype 校验符合 spec；输出 format=Norm、stride 走默认步幅。
- test/operation/test_operation_nn.py: OP-FC-001~007 覆盖无 bias/可选 bias、类型错误、rank/feature mismatch、bias 形状错误、space mismatch 与链式兼容性。
结论: 通过。未发现 spec/实现/测试闭环不一致。
