时间：2026-04-02 08:28:27 +0800
任务：T-20260402-d5798e12
任务目标：按 `dsl_ast_import_bound_helper_plan.md` 的 `A1`，仅修改 `spec/dsl/ast.md`，明确 helper 必须通过当前文件实际 import 绑定名才能被解析，并写清 alias import、direct symbol import、未导入裸 helper 三类情况，同时覆盖 `nn/dma/arch`。
改动：
- 核对任务前置：
  - `worktree=/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1` 可访问。
  - 当前无其他由睡觉小分队进行中的任务，已按要求向管理员同步“已开始、无阻塞、仅修改 spec/dsl/ast.md”。
- 更新 `spec/dsl/ast.md`：
  - 在 `限制与边界` 中新增总规则：AST 只认当前文件里实际 import 绑定进来的 helper 名，不再把未导入的裸 helper 名、未绑定的原始模块名或普通同名函数/变量偷当成 DSL helper。
  - 在 `parse_function(...)` 注意事项中把三类场景写清：
    - alias import：如 `import kernel_gen.operation.dma as cc` / `from kernel_gen.operation import dma as cc` 后，仅 `cc.slice(...)` / `cc.view(...)` / `cc.deslice(...)` 可解析；
    - direct symbol import：如 `from kernel_gen.operation.dma import slice as cut` 后，仅 `cut(...)` 可解析；
    - 未导入场景：裸 `slice(...)` / `view(...)` / `deslice(...)`、`nn.add(...)`、`get_dynamic_memory(...)`、`launch_kernel(...)`、`get_block_id()` 等未实际 import 绑定名不得继续被 AST 识别为 helper。
  - 明确上述 import 绑定名规则同时覆盖 `nn`、`dma`、`arch` 三组 helper，不能只对 `dma` 生效。
  - 在 `测试目标` 与 `功能与用例清单` 中新增 `AST-019/020/021`，冻结 alias import、direct symbol import、未导入裸 helper 三类行为及 `nn/dma/arch` 全覆盖口径，并标注为下游待补测试映射。
- 自检：
  - `rg -n "import 绑定名|alias|slice\\(|view\\(|deslice\\(|nn\\.add|get_dynamic_memory" spec/dsl/ast.md`（exit 0）
  - `git diff --check -- spec/dsl/ast.md`（exit 0）
结论：
- 已完成本任务要求，变更范围仅 `wt-20260402-dsl-ast-a1/spec/dsl/ast.md` 与本记录文件。
- 测试情况：本任务为 spec 阶段，未运行 `pytest`；已完成关键词校验与 diff 格式校验。
- 下一步建议：按最新管理员规则，不自行新建后续任务；建议由管理员创建 `A1` 的审查任务，继续沿 `审查/复审/合并` 链推进。
时间：2026-04-02 08:31:02 +0800
任务：T-20260402-f69da9d2
任务目标：修正 `dsl_ast A1`；仅修改 `spec/dsl/ast.md`，收回未导入裸 `arch/dma` helper 的旧成功/arity 路径映射，统一 helper 必须通过当前文件实际 import 绑定名才能被解析，并收敛相关错误优先级；覆盖 alias import、direct symbol import、未导入裸 helper 三类情况，同时覆盖 `nn/dma/arch`。
改动：
- 核对任务前置：
  - `worktree=/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1` 可访问。
  - 当前无阻塞，已向管理员同步开工状态。
- 更新 `spec/dsl/ast.md`：
  - 在 `parse_function(...)` 注意事项中新增错误优先级说明：未导入裸 helper 必须先落到“helper 未通过当前文件实际 import 绑定名引入”的入口错误；只有 helper 名已合法绑定后，才继续进入 arity/source/space 校验。
  - 在 `测试目标` 中删去“裸 `get_block_id/get_block_num/get_thread_id/get_thread_num/get_dynamic_memory/launch_kernel` 作为当前有效成功路径”的旧表述，改成“已通过 import 绑定名引入”的前提描述。
  - 在 `功能与用例清单` 中把 `AST-014A~P` 统一改写为“已通过 import 绑定名引入时才进入成功/arity/name/space 校验；未导入裸名不再复用旧成功/arity 路径”，从 spec 层收回裸 `arch/dma` helper 的旧成功映射。
  - 保留并对齐 `AST-019/020/021`，继续用其覆盖 alias import、direct symbol import、未导入裸 helper 三类行为，并同时覆盖 `nn/dma/arch`。
- 自检：
  - `rg -n "import 绑定名|alias|slice\\(|view\\(|deslice\\(|nn\\.add|get_dynamic_memory|get_block_id|launch_kernel" spec/dsl/ast.md`（exit 0）
  - `git diff --check -- spec/dsl/ast.md`（exit 0）
结论：
- 已完成本任务要求，变更范围仅 `wt-20260402-dsl-ast-a1/spec/dsl/ast.md` 与同链路记录文件。
- 测试情况：本任务为 spec 修正阶段，未运行 `pytest`；已完成关键词校验与 diff 格式校验。
- 下一步建议：按最新管理员规则，不自行新建后续任务；建议由管理员创建 `A1` 的复审/后续链路任务继续推进。
时间：2026-04-02 08:26:31 +0800
任务：T-20260402-b3625535
任务目标：只读审查 `dsl_ast A1`，复核 `spec/dsl/ast.md` 的 helper import 绑定名解析合同；覆盖 alias import、direct symbol import、未导入裸 helper 三类情况，并同时覆盖 `nn/dma/arch`；不改实现、测试或 lowering。
改动：
- 只读核对：
  - `wt-20260402-dsl-ast-a1/spec/dsl/ast.md`
  - `ARCHITECTURE/plan/dsl_ast_import_bound_helper_plan.md`
  - `wt-20260402-dsl-ast-a1/test/dsl/test_ast.py`
  - `wt-20260402-dsl-ast-a1/test/dsl/test_ast_visitor.py`
  - `wt-20260402-dsl-ast-a1/test/dsl/test_mlir_gen.py`
  - `wt-20260402-dsl-ast-a1/kernel_gen/dsl/ast.py`
- 复核命令：
  - `rg -n "import 绑定名|alias|slice\\(|view\\(|deslice\\(|nn\\.add|get_dynamic_memory" spec/dsl/ast.md`（exit `0`）
  - `git diff --check -- spec/dsl/ast.md`（exit `0`）
- 问题列表：
  - `P1`：`spec/dsl/ast.md` 在 helper import 绑定名规则上自相矛盾。文档前部 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L43) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L48) 以及 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L84) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L96) 明确写成“helper 只认当前文件实际 import 绑定名；未导入的裸 `slice/view/deslice`、`nn.add`、`get_dynamic_memory`、`launch_kernel`、`get_block_id` 等不得再被当成 helper 解析”。但同一文件的测试目标与用例映射 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L509) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L556) 仍把裸 `get_block_id()` / `get_block_num()` / `get_thread_id()` / `get_dynamic_memory(...)` / `launch_kernel(...)` 以及裸 `load(...)` / `slice(...)` 的旧口径保留为当前有效合同，并继续映射到现有测试；而现有测试本身确实没有导入这些 helper，例如 [test_mlir_gen.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_mlir_gen.py#L327), [test_ast_visitor.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast_visitor.py#L223), [test_ast_visitor.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast_visitor.py#L748), [test_ast.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast.py#L914) 都仍在用裸 helper 名。结果是 spec 同时宣称“未导入裸 helper 不再解析”和“裸 helper 继续走成功路径/arity 诊断路径”，会直接误导 A2/A3/I1 的入口前置条件与错误优先级。
- 风险核查：
  - 功能正确性风险：下游实现无法根据当前 spec 判断 bare `arch/dma` helper 应该被拒绝，还是继续触发旧的成功/arity 诊断路径。
  - 边界条件风险：对未导入裸 helper 的错误优先级未收口，`unimported helper` 与 `Unsupported <helper> arity` 两套口径并存。
  - 异常路径风险：`arch` 与 `dma` 的既有成功/负向映射没有同步降级为“下游待补测试映射”，会让审查/实现误以为现有测试仍然支撑旧入口。
  - 漏洞/歧义风险：helper 名绑定规则若不唯一，`emit_mlir/mlir_gen` 可能继续兜底识别未导入名字，违背本链路“AST 是唯一 helper 入口裁决层”的目标。
结论：
- `需修改`。
- 修改建议：
  - 只修改 `wt-20260402-dsl-ast-a1/spec/dsl/ast.md`。
  - 把 `arch/dma` 现有裸 helper 成功路径与 arity 负路径映射收敛到和 A1 新规则一致：要么明确这些用例的前提是“helper 已通过 import 绑定名引入”，并改写对应示例/映射；要么把与当前现有测试冲突的映射降级为“下游待补测试映射”，不能继续保留为当前有效口径。
  - 重点收口 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L48) 与 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L509) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L556) 的冲突。
- 按管理员新规则，本任务完成后不自行新建后续任务；如需进入下一步，请由管理员创建并分发。
时间：2026-04-02 08:38:56 +0800
任务：T-20260402-1c14e0f8
任务目标：再次修正 `dsl_ast A1`；只改 `spec/dsl/ast.md`，收回 `dma` 的 `AST-013A/AST-014` 中仍保留的裸 `load/slice` 旧 arity 成功映射，统一未导入裸 helper 先报入口错误，并保持 `nn/dma/arch` 三组都只通过当前文件实际 import 绑定名解析；不改 lowering、实现或测试。
改动：
- 修改文件：
  - `wt-20260402-dsl-ast-a1/spec/dsl/ast.md`
- 变更要点：
  - 将 `AST-013A` 收敛为“仅对已通过当前文件 import 绑定名引入的 `load` helper 进入 arity/source/space 校验；未导入裸 `load(...)` 不再复用旧路径”。
  - 将 `AST-014` 收敛为“仅对已通过当前文件 import 绑定名引入的 `slice` helper 进入 arity/source/space 校验；未导入裸 `slice(...)` 不再复用旧路径”。
  - 保持文档前部关于 alias import、direct symbol import、未导入裸 helper 三类情况的总规则不变，并继续覆盖 `nn/dma/arch` 三组 helper。
- 验证命令：
  - `rg -n "AST-013A|AST-014|import 绑定名|load\\(|slice\\(" spec/dsl/ast.md`（exit `0`）
  - `git diff --check -- spec/dsl/ast.md`（exit `0`）
- 测试执行：
  - 未运行 `pytest`；本轮仅做 spec 收口与文本一致性校验。
结论：
- `已完成`。
- 当前无阻塞。
- 按管理员新规则，本任务完成后不自行新建后续任务；如需继续推进，请由管理员创建并分发 A1 的后续审查/链路任务。
时间：2026-04-02 08:33:12 +0800
任务：T-20260402-b3625535
任务目标：复审 `dsl_ast A1` 修正 spec；只读复核 `spec/dsl/ast.md` 是否已收回未导入裸 `arch/dma` helper 的旧成功/arity 路径映射，并确认 helper 仅通过当前文件实际 import 绑定名解析；覆盖 alias import、direct symbol import、未导入裸 helper 三类情况，同时覆盖 `nn/dma/arch`。
改动：
- 只读核对：
  - `wt-20260402-dsl-ast-a1/spec/dsl/ast.md`
  - `ARCHITECTURE/plan/dsl_ast_import_bound_helper_plan.md`
  - `wt-20260402-dsl-ast-a1/test/dsl/test_ast.py`
  - `wt-20260402-dsl-ast-a1/test/dsl/test_ast_visitor.py`
  - `wt-20260402-dsl-ast-a1/test/dsl/test_mlir_gen.py`
- 复核命令：
  - `rg -n "import 绑定名|alias|slice\\(|view\\(|deslice\\(|nn\\.add|get_dynamic_memory" spec/dsl/ast.md`（exit `0`）
  - `git diff --check -- spec/dsl/ast.md`（exit `0`）
- 问题列表：
  - `P1`：本轮修正已经把裸 `arch` helper 的旧成功/arity 路径大部分从当前有效映射中收回，但仍未完整收回裸 `dma` helper 的旧 arity 路径。当前 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L84) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L90) 已明确“helper 只认当前文件实际 import 绑定名；未导入裸 helper 先报入口错误，不再复用旧成功/arity 路径”。`arch` 相关 `AST-014A~P` 也已改成“已通过 import 绑定名引入”或“下游待补测试映射”，见 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L532) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L547)。但 `dma` 相关的 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L530) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L531) 仍把 `load(...)` / `slice(...)` 的非法参数路径保留为当前有效映射，并继续指向现有测试；而这些测试源码仍是未导入的裸 helper 调用，见 [test/dsl/test_ast.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast.py#L914) 到 [test/dsl/test_ast.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast.py#L958) 与 [test/dsl/test_ast_visitor.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast_visitor.py#L3488) 到 [test/dsl/test_ast_visitor.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast_visitor.py#L3532)。这意味着 spec 仍然同时保留了“未导入裸 helper 先报入口错误”和“裸 `load/slice` 继续进入 arity/source/space 诊断”两套口径，A1 合同尚未完全收口。
- 风险核查：
  - 功能正确性风险：`dma` 裸 helper 的错误优先级仍不唯一，下游实现无法判断应先拒绝“未导入”还是继续做旧的 arity/source/space 诊断。
  - 边界条件风险：`nn/dma/arch` 三组规则表面上已统一，但 `dma` 仍保留旧映射，导致“同时覆盖三组 helper”在 spec 中实际上未完全成立。
  - 异常路径风险：若后续 `emit_mlir/mlir_gen` 继续沿用裸 `load/slice` 的旧入口，A1 想冻结的“AST 是唯一 helper 入口裁决层”边界仍会被稀释。
结论：
- `需修改`。
- 修改建议：
  - 只修改 `wt-20260402-dsl-ast-a1/spec/dsl/ast.md`。
  - 把 `AST-013A` 与 `AST-014` 这两条 `dma` 负向映射收敛到 A1 新规则下：要么明确前提是 helper 已通过 import 绑定名引入，要么把现有裸 helper 测试映射降级为“下游待补测试映射”。
  - 保持 alias import、direct symbol import、未导入裸 helper 三类规则对 `nn/dma/arch` 的表述一致，不再让 `dma` 残留旧口径。
- 按管理员新规则，本任务完成后不自行新建后续任务；如需进入下一步，请由管理员创建并分发。
时间：2026-04-02 08:41:39 +0800
任务：T-20260402-4d1ba8f7
任务目标：再次复审 `dsl_ast A1`；只读复核 `spec/dsl/ast.md` 是否已收回 `dma` 的 `AST-013A/AST-014` 中裸 `load/slice` 旧 arity 成功映射，并确认未导入裸 helper 一律先报入口错误，且 `nn/dma/arch` 三组都只通过当前文件实际 import 绑定名解析。
改动：
- 只读核对：
  - `wt-20260402-dsl-ast-a1/spec/dsl/ast.md`
  - `ARCHITECTURE/plan/dsl_ast_import_bound_helper_plan.md`
  - 当前链路记录文件
- 复核命令：
  - `git -C /home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1 diff --check -- spec/dsl/ast.md`（exit `0`）
- 复核结果：
  - [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L84) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L90) 已明确：helper 解析必须以前文件实际 import 绑定名为前提，未导入裸 helper 一律先报入口错误，只有已合法绑定后才进入 arity/source/space 校验。
  - `dma` 残留映射已收回：[spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L530) 与 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L531) 已改成“已通过当前文件 import 绑定名引入的 `load/slice` 才进入旧 arity/source/space 校验；未导入裸名不再复用该路径”，并降级为下游待补测试映射。
  - `arch` 相关 `AST-014A~P` 已保持同一口径，见 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L532) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L547)。
  - alias import、direct symbol import、未导入裸 helper 三类场景，以及 `nn/dma/arch` 三组统一规则，已集中写在 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L84) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L89) 与 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L550) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L552)，未再只偏向 `dma`。
- 风险核查：
  - 当前未再发现 `dma` 裸 helper 继续保留旧成功/arity 路径的 spec 冲突。
  - 当前未再发现 `nn/dma/arch` 三组在 import 绑定名规则上口径不一致的条目。
结论：
- `通过`。
- `spec/dsl/ast.md` 已收回 `dma` 的裸 `load/slice` 旧映射，并把 helper 解析统一收口到“当前文件实际 import 绑定名”规则下。
- 按管理员新规则，本任务完成后不自行新建后续任务；如需进入下一步，请由管理员创建并分发。
时间：2026-04-02 08:34:59 +0800
任务：T-20260402-3c2f8ff9
任务目标：复审 `dsl_ast A1` 修正 spec；只读复核 `spec/dsl/ast.md` 是否已收回未导入裸 `arch/dma` helper 的旧成功/arity 路径映射，并确认 helper 仅通过当前文件实际 import 绑定名解析；覆盖 alias import、direct symbol import、未导入裸 helper 三类情况，同时覆盖 `nn/dma/arch`。
改动：
- 只读核对：
  - `wt-20260402-dsl-ast-a1/spec/dsl/ast.md`
  - `ARCHITECTURE/plan/dsl_ast_import_bound_helper_plan.md`
  - `wt-20260402-dsl-ast-a1/test/dsl/test_ast.py`
  - `wt-20260402-dsl-ast-a1/test/dsl/test_ast_visitor.py`
  - `wt-20260402-dsl-ast-a1/test/dsl/test_mlir_gen.py`
- 复核命令：
  - `rg -n "import 绑定名|alias|slice\\(|view\\(|deslice\\(|nn\\.add|get_dynamic_memory" spec/dsl/ast.md`（exit `0`）
  - `git diff --check -- spec/dsl/ast.md`（exit `0`）
- 问题列表：
  - `P1`：本轮修正已把裸 `arch` helper 的旧成功/arity 路径大部分从当前有效映射中收回，但仍未完整收回裸 `dma` helper 的旧 arity 路径。当前 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L84) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L90) 已明确“helper 只认当前文件实际 import 绑定名；未导入裸 helper 先报入口错误”。`arch` 相关 `AST-014A~P` 也已改成“已通过 import 绑定名引入”或“下游待补测试映射”，见 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L532) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L547)。但 `dma` 相关的 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L530) 到 [spec/dsl/ast.md](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/spec/dsl/ast.md#L531) 仍把 `load(...)` / `slice(...)` 的非法参数路径保留为当前有效映射，并继续指向现有测试；而这些测试源码仍是未导入的裸 helper 调用，见 [test/dsl/test_ast.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast.py#L914) 到 [test/dsl/test_ast.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast.py#L958) 与 [test/dsl/test_ast_visitor.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast_visitor.py#L3488) 到 [test/dsl/test_ast_visitor.py](/home/lfr/kernelcode_generate/wt-20260402-dsl-ast-a1/test/dsl/test_ast_visitor.py#L3532)。因此 spec 仍然同时保留了“未导入裸 helper 先报入口错误”和“裸 `load/slice` 继续进入 arity/source/space 诊断”两套口径，A1 合同尚未完全收口。
- 风险核查：
  - 功能正确性风险：`dma` 裸 helper 的错误优先级仍不唯一，下游实现无法判断应先拒绝“未导入”还是继续做旧的 arity/source/space 诊断。
  - 边界条件风险：`nn/dma/arch` 三组规则表面上已统一，但 `dma` 仍保留旧映射，导致“同时覆盖三组 helper”在 spec 中实际上未完全成立。
  - 异常路径风险：若后续 `emit_mlir/mlir_gen` 继续沿用裸 `load/slice` 的旧入口，A1 想冻结的“AST 是唯一 helper 入口裁决层”边界仍会被稀释。
结论：
- `需修改`。
- 修改建议：
  - 只修改 `wt-20260402-dsl-ast-a1/spec/dsl/ast.md`。
  - 把 `AST-013A` 与 `AST-014` 这两条 `dma` 负向映射收敛到 A1 新规则下：要么明确前提是 helper 已通过 import 绑定名引入，要么把现有裸 helper 测试映射降级为“下游待补测试映射”。
  - 保持 alias import、direct symbol import、未导入裸 helper 三类规则对 `nn/dma/arch` 的表述一致，不再让 `dma` 残留旧口径。
- 按管理员新规则，本任务完成后不自行新建后续任务；如需进入下一步，请由管理员创建并分发。
