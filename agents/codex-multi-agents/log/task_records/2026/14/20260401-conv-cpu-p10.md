时间：2026-04-01 03:41:40 +0800
任务：T-20260401-d792e53e
任务目标：按 `ARCHITECTURE/plan/conv_cpu_tiled_v1_plan.md` 当前最直接的下一步，在 `kernel_gen/dsl/emit_c.py` 补齐 `img2col2d + DMA + tiled compute` 所需节点级映射，并补 `test/dsl/test_emit_c.py` 覆盖；仅做本步，不修改 `gen_kernel`，不引入 pass 旁路。
改动：
- 已确认 `worktree` 为 `/home/lfr/kernelcode_generate/wt-20260401-conv-p10`，目标文件为 `kernel_gen/dsl/emit_c.py` 与 `test/dsl/test_emit_c.py`。
- 当前记录文件不存在，本次任务内创建并开始按执行顺序追加。
- 状态核对：`agents-lists.md` 当前显示本人为 `busy`；`../TODO.md` 的“正在执行的任务”区仅有本任务 `T-20260401-d792e53e` 指派给本人。另有 `T-20260401-c734176e` 位于任务列表区，尚未进入进行中，不构成并行执行。
- 初步排查结果：
  - `kernel_gen/dsl/emit_c.py` 当前仅支持 `arith.*`、`symbol.add`、`scf.for`、`dma.load/store`，与计划中的当前断点一致。
  - `test/dsl/test_emit_mlir.py::test_emit_mlir_img2col2d_with_loop_slice_deslice_lowering` 对应的真实 lowering 形态为 `symbol.for -> arith.constant* -> dma.alloc -> dma.slice -> nn.img2col2d -> arith.constant* -> dma.deslice -> func.return`。
  - 当前 `emit_c` 尚未覆盖 `symbol.for`、`dma.alloc`、`dma.slice`、`dma.deslice`、`dma.view`、`nn.img2col2d`，这是本轮实现收口的最小必要集合。
结论：
- 已开始执行，无其他进行中的实现/测试任务与本任务并行。
- 下一步：补齐上述 op 的最小 CPU 文本映射，优先让 `symbol.for + dma.alloc + dma.slice + nn.img2col2d + dma.deslice` 链路在 `emit_c` 可输出稳定文本，再补 `dma.view` 节点测试。
时间：2026-04-01 03:49:17 +0800
任务：T-20260401-d792e53e
任务目标：完成 `emit_c.py` 对 `img2col2d + DMA + tiled compute` 最小必要节点的 CPU 文本映射，并补 `test_emit_c.py` 覆盖；保持 `gen_kernel` 与 pass 链路不变。
改动：
- `kernel_gen/dsl/emit_c.py`
  - 新增 `f32 -> float` 的最小类型映射，解除 float tile/local buffer 与 `arith.addf` 的发射阻塞。
  - 新增 `symbol.for` 循环发射，复用现有 `for (long long ... )` 文本风格，并过滤循环体中的空语句，避免常量节点产生空行。
  - 新增 `dma.alloc` 文本映射：发射 `shape/stride` 本地数组与 `Memory<...>` 局部声明，覆盖 conv 链中的 tile-local buffer。
  - 新增 `dma.view` 文本映射：发射 `offsets/sizes/strides` 数组与 `view(...)` 调用。
  - 新增 `dma.slice` / `dma.deslice` 文本映射：发射 `Vector(...)` 参数缓冲区与 `slice(...)` / `deslice(...)` 调用。
  - 新增 `nn.img2col2d` 文本映射：先声明结果 `Memory<float>`，再发射 `cpu::img2col2d(...)` 调用。
  - 扩展 memory SSA 命名：允许 `dma.alloc`、`dma.view`、`nn.img2col2d`、`dma.deslice` 结果被后续节点稳定引用。
- `test/dsl/test_emit_c.py`
  - 更新算术用例，新增 `arith.addf` 正向断言，锁定 float tile-local compute 的最小支持。
  - 调整“unsupported type”负例，改为非法 `NnMemoryType.element_type` 的 `dma.alloc` 场景，避免与本次新增的 `f32` 支持冲突。
  - 新增 `test_emit_c_op_lowers_dma_alloc_and_view`，覆盖 `dma.alloc` 与 `dma.view` 独立节点映射。
  - 新增 `test_emit_c_op_lowers_img2col2d_dma_loop_pipeline`，通过真实 `build_func_op_from_ast(...)` 产物锁定 `symbol.for + dma.alloc + dma.slice + nn.img2col2d + dma.deslice` 链路文本片段。
结论：
- P10 本轮实现/测试完成，改动仅限 `kernel_gen/dsl/emit_c.py`、`test/dsl/test_emit_c.py` 与当前记录文件；未修改 `kernel_gen/dsl/gen_kernel.py`，未新增 pass/旁路。
- 验证通过：
  - `pytest -q test/dsl/test_emit_c.py` -> `11 passed in 0.28s`
  - `PYTHONPATH=. pytest -q test/operation/test_operation_nn.py -k 'img2col1d or img2col2d'` -> `2 passed, 63 deselected in 0.30s`
  - `pytest -q test/dialect/test_nn_dialect.py -k 'img2col1d or img2col2d'` -> `2 passed, 79 deselected in 0.28s`
  - `pytest -q test/dsl/test_emit_mlir.py -k 'img2col1d or img2col2d'` -> `2 passed, 57 deselected in 0.35s`
  - `PYTHONPATH=. pytest -q test/include/cpu/test_nn.py -k 'img2col1d or img2col2d'` -> `5 passed, 23 deselected in 0.59s`
- 说明：计划中的 `pytest -q test/dsl/test_mlir_gen.py -k 'img2col'` 在当前仓库无命中用例（exit 5）；已改用当前最接近的同链路基线 `pytest -q test/dsl/test_mlir_gen.py -k test_build_func_op_supports_symbolic_for_loop_dma_without_return`，结果 `1 passed, 107 deselected in 0.27s`。
- 下一步建议：按同计划链新建审查任务，重点只读复核 `emit_c` 新增的 `symbol.for/dma.alloc/view/slice/deslice/nn.img2col2d` 文本映射是否与当前 CPU 公开接口口径一致，确认本轮未提前混入 `gen_kernel` 结构化装配逻辑。
时间：2026-04-01 03:51:02 +0800
任务：T-20260401-d792e53e
任务目标：按协作流程完成 `-done`、新建同链路后续审查任务，并向管理员回报本轮结论与测试结果。
改动：
- 使用主仓真实协作文件更新 `agents/codex-multi-agents/agents-lists.md`，将本人介绍同步为“实现/测试: conv emit_c、gen_kernel、DSL回归补测”。
- 使用 `codex-multi-agents-task.sh -done` 将 `T-20260401-d792e53e` 从主仓 `TODO.md` 迁入 `DONE.md`。
- 新建同链路后续审查任务 `T-20260401-189a5def`，沿用 `worktree=/home/lfr/kernelcode_generate/wt-20260401-conv-p10` 与当前记录文件。
- 使用 `codex-multi-agents-tmux.sh -talk` 向管理员 `神秘人` 回报完成情况、涉及文件、测试结果，以及 `test/dsl/test_mlir_gen.py -k img2col` 在当前仓库无命中用例、已改用等价基线复测的说明。
结论：
- 当前任务已按要求完成收尾，`T-20260401-d792e53e` 已进入主仓 `DONE.md`。
- 后续任务已创建：`T-20260401-189a5def`（P10 审查任务，待管理员分发）。
- 主仓状态已同步：`agents-lists.md` 中本人状态为 `free`，回报消息已写入 `agents/codex-multi-agents/log/talk.log`。

时间：2026-04-01 04:44:29 +0800
任务：T-20260401-189a5def
任务目标：只读复核 `kernel_gen/dsl/emit_c.py` 新增的 `symbol.for`、`dma.alloc/view/slice/deslice`、`nn.img2col2d` 与 `f32` 文本映射，以及 `test/dsl/test_emit_c.py` 对应覆盖；确认仅收口 `emit_c`，未提前混入 `gen_kernel` 或 pass。
改动：
- 复核 `ARCHITECTURE/plan/conv_cpu_tiled_v1_plan.md`、`kernel_gen/dsl/emit_c.py`、`test/dsl/test_emit_c.py`、`spec/dsl/emit_c.md`、`spec/include/cpu/cpu.md`、`include/cpu/Memory.h`、`include/cpu/Nn.h`、`include/api/Core.h` 与 `kernel_gen/dsl/gen_kernel.py`，确认当前未提交改动仅位于 `kernel_gen/dsl/emit_c.py`、`test/dsl/test_emit_c.py` 与本记录文件；`gen_kernel` / `kernel_gen/passes` / `spec` 未被本轮实现改动。
- 复测：`pytest -q test/dsl/test_emit_c.py`，exit code=0（`11 passed in 0.27s`）。
- 额外核对 CPU 公开接口：`spec/include/cpu/cpu.md` 当前只冻结 `include/cpu/Memory.h` 与 `include/cpu/Nn.h` 的公开接口，并明确 `cpu::img2col2d(...)` 仅依赖 `cpu::Memory<T>` 与 `long long` 标量参数；仓库 C/C++ 侧仅在 `include/api/Core.h` 提供 `Vector`，未发现 `view/slice/deslice` 的 C/C++ 声明或实现。
- 本地最小复现：对同一 `dma.alloc` 结果连续发射两条 `DmaSliceOp` 时，两次结果都生成 `v0_offsets/v0_sizes/v0_strides`，确认 `emit_c` 当前会在同一作用域复用相同临时变量名。
结论：
- 问题列表：
  - `[P0] [kernel_gen/dsl/emit_c.py](kernel_gen/dsl/emit_c.py) 的 [_emit_memory_decl](kernel_gen/dsl/emit_c.py) / [_emit_img2col2d_stmt](kernel_gen/dsl/emit_c.py) 当前统一把 `dma.alloc` 与 `nn.img2col2d` 结果生成为 `Memory<float>(static_cast<float*>(nullptr), ...)`。但 [include/cpu/Memory.h](include/cpu/Memory.h) 的 `Memory::at(...)` 会直接解引用 `data_`，而 [include/cpu/Nn.h](include/cpu/Nn.h) 的 `cpu::img2col2d(...)` 在主循环中会立即执行 `out.at(...) = value.at(...)`。风险：即使后续生成代码成功编译，也会在运行时直接踩空指针，当前 `test_emit_c.py` 只断言字符串，未覆盖这一点。建议：收口 `dma.alloc` / `img2col2d` 的 backing 语义，保证生成文本提供有效存储或改为当前 CPU 公开接口可承接的输出写回方式。`
  - `[P1] [kernel_gen/dsl/emit_c.py](kernel_gen/dsl/emit_c.py) 的 [_emit_dma_view_stmt](kernel_gen/dsl/emit_c.py)、[_emit_dma_slice_stmt](kernel_gen/dsl/emit_c.py)、[_emit_dma_deslice_stmt](kernel_gen/dsl/emit_c.py) 现在会直接发射 `view(...)` / `slice(...)` / `deslice(...)`。但 [spec/include/cpu/cpu.md](spec/include/cpu/cpu.md) 当前只冻结 `Memory/Nn` 接口，且仓库 C/C++ 侧未发现这三个 helper 的声明/实现。风险：P10 生成源码会落到仓库当前不存在的目标侧符号，后续 compile/run smoke 无法通过。建议：要么把 `emit_c` 文本改写为当前已存在的 CPU 公开接口，要么先独立补齐对应公开契约与 C++ 实现后再放行。`
  - `[P1] [kernel_gen/dsl/emit_c.py](kernel_gen/dsl/emit_c.py) 的 [_emit_dma_vector_buffers](kernel_gen/dsl/emit_c.py) 由 `target_expr/source_expr` 拼接临时数组名，导致同一作用域内对同一个 tile 连续发射两条 `dma.slice` / `dma.deslice` 时重复生成同名 `*_offsets/*_sizes/*_strides`。风险：同一函数体内会出现局部变量重定义，编译失败；当前 [test/dsl/test_emit_c.py](test/dsl/test_emit_c.py) 仅覆盖单条 slice/deslice 链路，未锁住重复场景。建议：改为基于 op 实例或独立计数器分配唯一辅助变量名，并补相应回归测试。`
- 漏洞排查结果：
  - 输入校验绕过：未发现新增对 `DmaAllocOp/DmaViewOp/NnImg2col2dOp` 类型判断的直接绕过；但对“目标侧 helper 是否存在”的契约缺少校验，错误会延后到更后面的代码生成/编译阶段暴露。
  - 类型/形状绕过：`NnMemoryType` 与 `f32` 的最小映射已补齐，但 `dma.alloc/img2col2d` 生成的空指针 backing 破坏了 CPU include 层对有效 `Memory` 视图的前置假设。
  - 边界越界：`cpu::img2col2d(...)` 会通过 `Memory::at(...)` 访问输入/输出元素；当前空指针 backing 会直接触发越界/崩溃风险。
  - 错误处理缺失：对未定义 `view/slice/deslice` 目标接口与重复辅助变量名都没有在 `emit_c` 阶段前置报错。
  - 状态污染：重复 `slice/deslice` 复用同名辅助数组，存在同一作用域下的名称污染与重定义风险。
  - 资源释放问题：`dma.alloc` 当前未分配任何实际 backing store，仅构造空视图，不满足“分配”语义。
- 改进建议：已转为后续修复任务，要求在同一 `worktree` 内仅收口 `emit_c/test`，先解决上述三项问题后再复审；未发现可独立于本次修复之外的额外改进点。
- 最终结论：`需修改`。当前不应进入下一阶段放行；建议新建同链路实现/测试修复任务，继续限制在 `kernel_gen/dsl/emit_c.py` 与 `test/dsl/test_emit_c.py`，禁止提前改 `gen_kernel` 或 pass。

时间：2026-04-01 04:56:53 +0800
任务：T-20260401-3e575bec
任务目标：仅在 `emit_c/test` 收口三项问题：1）停止生成 CPU 侧不存在的 `view/slice/deslice` helper；2）避免 `dma.alloc` 与 `nn.img2col2d` 结果为 `nullptr` backing；3）修复重复 `dma.slice/dma.deslice` 的辅助变量重名并补回归测试；继续禁止修改 `gen_kernel/pass`。
改动：
- `kernel_gen/dsl/emit_c.py`
  - 为 `EmitCContext` 增加独立临时名分配器，避免重复 `dma.slice/dma.deslice` 在同一作用域复用相同辅助变量名。
  - 扩展 `_emit_memory_decl(...)`：支持显式 `data/format/space` 与 backing storage 发射。
  - `dma.alloc` 与 `nn.img2col2d` 结果不再生成为 `nullptr` backing 的 `Memory`，静态 shape 直接发射零初始化本地 buffer，动态 shape 退化为 `new[]` backing。
  - `dma.view` 改为基于 `source.data()/stride()/format()/space()` 直接构造 `Memory` 视图，不再生成 CPU 侧不存在的 `view(...)` helper。
  - `dma.slice` / `dma.deslice` 改为显式嵌套循环 + `Memory::at(...)` 拷贝/回写，不再生成 CPU 侧不存在的 `slice(...)` / `deslice(...)` helper。
- `test/dsl/test_emit_c.py`
  - 更新 `dma.alloc/dma.view` 断言，锁定真实 backing storage 与直接 `Memory` 视图构造文本。
  - 更新 `img2col2d + DMA` pipeline 断言，锁定显式 buffer、显式 copy loop，并确保输出文本不再包含 `slice/deslice` helper 与 `nullptr`。
  - 新增重复 `dma.slice/dma.deslice` 的命名回归测试，锁定辅助变量唯一性。
结论：
- 三项目标已收口完成；改动仅位于 `kernel_gen/dsl/emit_c.py`、`test/dsl/test_emit_c.py` 与当前记录文件，`gen_kernel` / `pass` 未改动。
- 验证通过：
  - `pytest -q test/dsl/test_emit_c.py` -> `12 passed in 0.26s`
  - `pytest -q test/dsl/test_mlir_gen.py -k test_build_func_op_supports_symbolic_for_loop_dma_without_return` -> `1 passed, 107 deselected in 0.24s`
- 下一步建议：进入同链路审查，重点复核 `emit_c` 新增的 CPU 文本是否与现有 `Memory/img2col2d` 公开接口保持一致，并确认动态 shape fallback/backing 生命周期在当前阶段可接受。

时间：2026-04-01 10:00:55 +0800
任务：T-20260401-1f46f279
任务目标：conv P10 审查：沿用 wt-20260401-conv-p10 与同一记录文件，只读复核 kernel_gen/dsl/emit_c.py 与 test/dsl/test_emit_c.py 本轮三项修复；确认 CPU 侧不再生成 view/slice/deslice helper、dma.alloc/img2col2d 具备有效 backing、重复 dma.slice/dma.deslice 辅助变量唯一，继续禁止修改 gen_kernel/pass。
改动：
- 环境与边界核对：
  - worktree：`/home/lfr/kernelcode_generate/wt-20260401-conv-p10`
  - `git status` 仅见改动：[`kernel_gen/dsl/emit_c.py`](kernel_gen/dsl/emit_c.py)、[`test/dsl/test_emit_c.py`](test/dsl/test_emit_c.py) 与本记录文件；`kernel_gen/dsl/gen_kernel.py` / `kernel_gen/passes` 未改动。
- 三项修复点复核（只读）：
  1) 去除 CPU 侧 `view/slice/deslice` helper：`emit_c` 已改为直接构造 `Memory` 视图与显式嵌套循环拷贝（见 `_emit_dma_view_stmt` / `_emit_dma_copy_loop_nest`）；测试 EC-010 断言 `slice(`/`deslice(` 不出现在输出文本。
  2) `dma.alloc`/`nn.img2col2d` backing 有效：`_emit_dma_alloc_stmt` 与 `_emit_img2col2d_stmt` 均通过 `_emit_memory_decl(..., with_backing_storage=True)` 生成 buffer（静态 shape 为本地数组）；测试 EC-009/EC-010 断言输出包含 `*_buffer[...] = {};` 且不含 `nullptr`。
  3) 重复 `dma.slice/dma.deslice` 辅助变量名唯一：`EmitCContext.allocate_temp_name` 提供全局计数器，`_emit_dma_copy_loop_nest` 以 `dma{N}` 前缀生成 `*_src_indices/*_dst_indices`；测试 EC-011 覆盖并通过。
- 复测：
  - `pytest -q test/dsl/test_emit_c.py`，exit code=0（`12 passed in 0.26s`）。
  - `pytest -q test/dsl/test_mlir_gen.py -k test_build_func_op_supports_symbolic_for_loop_dma_without_return`，exit code=0（`1 passed, 107 deselected in 0.25s`）。
- spec/注释一致性核对：
  - [`spec/dsl/emit_c.md`](spec/dsl/emit_c.md) 仍限定“仅支持 arith/cmpi/scf.for/unit-tile dma.load/store/symbol.add”，与本轮实现/测试新增的 `symbol.for`、`dma.alloc/view/slice/deslice`、`nn.img2col2d` 支持不一致；且 spec 的用例清单未包含 EC-009~EC-011。
  - [`kernel_gen/dsl/emit_c.py`](kernel_gen/dsl/emit_c.py) 本轮新增/修改了多处函数/方法（例如 `EmitCContext.allocate_temp_name`、`_emit_dma_copy_loop_nest`、`_emit_backing_storage_decl`、`_emit_dma_alloc_stmt` 等），但未补齐符合审查规范的**中文**函数注释（至少“功能说明+使用示例”）。
结论：
- 问题列表：
  - `[P1] [spec/dsl/emit_c.md](spec/dsl/emit_c.md) 与实现/测试不一致：spec 未覆盖本轮新增 op/用例，且存在“仅新增 symbol.add”与实现支持 \`SymbolForOp\` 的口径冲突。风险：链路 spec/测试映射失真，后续审计/回归无法以 spec 为准。建议：补齐 spec 对新增 op/限制/错误语义与 EC-009~EC-011 测试映射。`
  - `[P2] [kernel_gen/dsl/emit_c.py](kernel_gen/dsl/emit_c.py) 新增/修改函数缺少中文注释与使用示例。风险：维护与审查基线不满足 [审查规范](agents/standard/审查规范.md) “注释一致性”门槛。建议：为本轮新增/修改函数/方法补齐中文注释（至少功能说明+使用示例），并补齐关联文件链接字段（参考 kernel_gen/dialect/dma.py 风格）。`
  - `[P2] [kernel_gen/dsl/emit_c.py](kernel_gen/dsl/emit_c.py) 动态 shape（type.shape 非全 IntAttr）时会发射 \`new[]\` backing，但当前 emit_c 片段未生成释放逻辑。风险：生成代码潜在内存泄漏。建议：明确阶段策略：要么暂时禁止动态 shape 并在 emit_c 抛错，要么改为 RAII 容器/在函数级生成释放。`
- 漏洞排查结果（6 类）：
  - 输入校验绕过：`emit_c` 对关键 op/type 仍有 `isinstance` 与布局静态约束（如 unit-tile/load/store）；未发现新增绕过路径，但 spec 缺失会导致校验口径不透明。
  - 类型/形状绕过：`_format_static_layout` 仍只接受 `IntAttr`；`dma.alloc` 使用 `op.dynamic_shape` 参与 shape 表达式发射，需依赖 dialect verifier 保证 rank/layout 一致。
  - 边界越界：`dma.slice/deslice` 通过 `Memory::at(...)` 访问；越界主要由 shape/stride/offsets/sizes/strides 不一致触发，当前无额外越界防护（依赖上游 verifier 与生成 IR 正确性）。
  - 错误处理缺失：对不支持的 op/type 仍抛 `EmitCError`；但对 spec 未覆盖的新 op 行为缺少文档约束，错误语义难以一致复现。
  - 状态污染：`allocate_temp_name` 引入独立计数器，已缓解同作用域重名污染风险；未见新的全局状态写入。
  - 资源释放问题：静态 shape 为栈上数组；动态 shape 使用 `new[]` 目前无 `delete[]` 路径，存在泄漏风险（见问题列表）。
- 改进建议：除上述必须修改项外，未发现额外改进点。
- 最终结论：`需修改`。下一步：新建同链路修复任务，优先补齐 `spec/dsl/emit_c.md` 与 `kernel_gen/dsl/emit_c.py` 的注释/示例/用例清单对齐，并明确动态 backing 生命周期策略；修复后再进入复审/合并。

时间：2026-04-01 10:44:07 +0800
任务：T-20260401-7fcf631f
任务目标：对齐 `emit_c` 的 spec/实现/测试：补齐 `symbol.for`、`dma.alloc/view/slice/deslice`、`nn.img2col2d` 的最小 CPU 发射规范与 EC-009~EC-011；为 `emit_c.py` 本轮新增/修改函数补中文注释（功能说明+使用示例+关联文件链接）；明确动态 shape backing 生命周期策略避免 `new[]` 泄漏；保持 `gen_kernel/pass` 不变。
改动：
- [`spec/dsl/emit_c.md`](spec/dsl/emit_c.md)
  - 更新最小支持 op 列表：加入 `symbol.for`、`dma.alloc/view/slice/deslice`、`nn.img2col2d`；修正“仅新增 symbol.add”过时描述。
  - 补齐 EC-009~EC-011 与测试映射，并补充约束：`dma.slice/dma.deslice` 必须显式 loop copy；backing storage 仅支持静态 shape，动态 shape 必须报错。
- [`kernel_gen/dsl/emit_c.py`](kernel_gen/dsl/emit_c.py)
  - 新增/完善 CPU 发射：`SymbolForOp`、`DmaAllocOp`、`DmaViewOp`、`DmaSliceOp`、`DmaDesliceOp`、`NnImg2col2dOp`；`dma.slice/dma.deslice` 通过显式 loop nest copy，避免外部 helper 依赖。
  - 明确动态 shape 策略：`_emit_backing_storage_decl` 在 `type.shape` 非全 `IntAttr` 时抛 `EmitCError`，避免 `new[]` 无释放路径导致泄漏。
  - 为本轮新增/修改的 helper 与入口补齐中文 docstring（功能说明+使用示例+关联文件链接），并更新文件级 `最后一次更改`。
- [`test/dsl/test_emit_c.py`](test/dsl/test_emit_c.py)
  - 新增 EC-009~EC-011 用例：覆盖 `dma.alloc/view`、`symbol.for + dma.alloc + dma.slice + nn.img2col2d + dma.deslice` 链路、重复 slice/deslice 辅助变量命名唯一。
  - 在 EC-009 内补充动态 shape alloc 负例：断言报错 `dynamic shape backing is unsupported`，锁定“暂禁动态 backing”策略。
  - 更新本轮改动测试注释元信息（创建者/最后一次更改/最近运行时间）。
结论：
- 变更仅涉及：[`spec/dsl/emit_c.md`](spec/dsl/emit_c.md)、[`kernel_gen/dsl/emit_c.py`](kernel_gen/dsl/emit_c.py)、[`test/dsl/test_emit_c.py`](test/dsl/test_emit_c.py) 与本记录文件；未改动 `kernel_gen/dsl/gen_kernel.py`、`kernel_gen/passes/*` 与 expectation。
- 复测通过：`pytest -q test/dsl/test_emit_c.py` -> `12 passed in 0.25s`。
- 下一步建议：新建同链路“合并任务”，仅合并上述 3 个业务文件 + 记录文件；合并前复核动态 shape 报错策略是否满足上游期望（如需支持 dynamic backing，需转到函数级 RAII/释放策略再扩展）。
