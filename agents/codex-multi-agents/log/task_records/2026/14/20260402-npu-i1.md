时间：2026-04-02 07:13:46 +0800
任务：T-20260402-f333980b
任务目标：在 `wt-20260402-npu-i1` 中仅修改 `kernel_gen/dsl/gen_kernel.py` 与 `test/dsl/test_gen_kernel.py`，实现 `target="npu_demo"` 的 body-level kernel 骨架，并补对应 `gen_kernel` 验收测试；要求显式包含 `KernelContext`、`thread_id/thread_num`、`TSM/TLM dynamic memory`、`view/slice/deslice/add`，不回退到 `.view/load/store/launch/barrier/arch.launch_kernel`。
改动：
- 任务前置核对：
  - `worktree=/home/lfr/kernelcode_generate/wt-20260402-npu-i1` 可访问。
  - 已查看主仓 `TODO.md` 的 `-doing` 列表，当前我名下只有本任务在执行。
  - 当前链路记录文件在主仓 `agents/codex-multi-agents/log/task_records/2026/14/20260402-npu-i1.md` 下新建。
- 规范与边界核对：
  - 读取 `ARCHITECTURE/plan/npu_demo_backend_plan.md` 与 `wt-20260402-npu-i1/spec/dsl/gen_kernel.md`，确认 `N3` 已冻结 `npu_demo` 的函数级 body-level kernel：签名必须包含 `npu_demo::KernelContext& ctx`，函数体顺序固定为 `thread_id/thread_num -> get_dynamic_memory(TSM/TLM) -> view -> slice -> add -> deslice`。
  - 读取 `wt-20260402-npu-i1/kernel_gen/dsl/gen_kernel.py`、`wt-20260402-npu-i1/test/dsl/test_gen_kernel.py`、`wt-20260402-npu-i1/kernel_gen/dsl/emit_c.py`、`wt-20260402-npu-i1/test/dsl/test_emit_c.py`，确认节点级 `npu_demo` 文本口径已存在，缺口只在函数级签名与 body 骨架。
  - 收到链路边界同步：DSL/AST helper 入口只允许“当前文件显式从对应 operation/dma 模块导入的 helper 名称”走 helper 解析；本任务未触达 `mlir_gen/ast_visitor/emit_mlir`，未引入新的 helper 解析路径，仅在记录中显式注明该边界未触发。
- 实现 `kernel_gen/dsl/gen_kernel.py`：
  - 新增 `_is_npu_demo_body_level_kernel(...)`，将 `target="npu_demo"` 的函数级固定骨架与通用 `emit_c` 拼装路径隔离。
  - 新增 `_get_npu_demo_body_level_kernel_types(...)`，固定当前支持子集为 `ctx + source -> out`，要求首参名显式为 `ctx`，`source/out` 都是 `NnMemoryType` 且 `element_type` 一致。
  - 新增 `_validate_npu_demo_body_level_kernel_body(...)`，锁定当前固定骨架只接受空 body，避免未来把真实 body 静默吞掉。
  - 新增 `_emit_npu_demo_body_level_kernel_body(...)`，直接生成固定源码骨架：
    - `npu_demo::KernelContext& ctx`
    - `ctx.thread_id()` / `ctx.thread_num()`
    - `ctx.get_dynamic_memory<float>(MemorySpace::TSM/TLM)`
    - `view(source, tid * 16, 16, 1)`
    - `slice(work_tile, src_view, 0, 16, 1)`
    - `add(work_tile, work_tile, out_tile)`
    - `deslice(out_tile, out, tid * 16, 16, 1)`
  - 在 `gen_signature(...)` 中为 `target="npu_demo"` 增加签名特判，首参强制生成为 `npu_demo::KernelContext& ctx`，随后保持只读 `Memory` 输入和显式 `Memory` 输出参数。
  - 在 `gen_body(...)` 中为 `target="npu_demo"` 增加固定 body 骨架特判，不回退到 generic `emit_c` 遍历。
- 补齐 `test/dsl/test_gen_kernel.py`：
  - 新增 `_npu_ctx()` 测试上下文。
  - 新增 `test_gen_kernel_emits_npu_demo_body_level_kernel`，验证 `KernelContext` 首参、`ctx.thread_id()` / `ctx.thread_num()` 与禁止词 `launch/barrier/arch.launch_kernel`。
  - 新增 `test_gen_kernel_emits_npu_demo_memory_pipeline`，验证 `TSM/TLM dynamic memory` 与 `view/slice/add/deslice` 的固定顺序，并禁止 `.view<`、`load<`、`store<`、表达式式 `slice(source` 与 `arch.launch_kernel`。
- 验证：
  - `pytest -q /home/lfr/kernelcode_generate/wt-20260402-npu-i1/test/dsl/test_gen_kernel.py -k 'npu_demo or direct_return_nn_add or conv2d_img2col2d_tiled or symbol_scalar_return or returns_target_source or uses_readonly_memory_inputs or lowers_memory_result_to_out_param or preserves_scalar_arg_order or emits_ops_in_order or assembles_loop_body or propagates_emit_c_error or rejects_unsupported_return_form or preserves_function_and_arg_names or supports_float32_scalar_and_memory'`（exit 0，`20 passed, 2 deselected in 0.49s`）
  - `pytest -q /home/lfr/kernelcode_generate/wt-20260402-npu-i1/test/dsl/test_gen_kernel.py`（exit 0，`22 passed in 0.35s`）
  - `git -C /home/lfr/kernelcode_generate/wt-20260402-npu-i1 diff --check -- kernel_gen/dsl/gen_kernel.py test/dsl/test_gen_kernel.py`（exit 0）
  - `git -C /home/lfr/kernelcode_generate/wt-20260402-npu-i1 status --short -- kernel_gen/dsl/gen_kernel.py test/dsl/test_gen_kernel.py` 显示仅本任务两处业务文件变更。
结论：
- 已完成本任务要求，改动范围仅 `wt-20260402-npu-i1/kernel_gen/dsl/gen_kernel.py` 与 `wt-20260402-npu-i1/test/dsl/test_gen_kernel.py`。
- `npu_demo` 的函数级 `gen_kernel` 现在会显式生成 `KernelContext/thread_id/thread_num/TSM-TLM dynamic memory/view/slice/deslice/add` 固定骨架，并保持禁止词不出现。
- AST/helper 新边界本任务未触发；未改 `emit_c/gen_kernel spec 以外` 的模块，也未把裸同名 helper 引入解析路径。
- 阻塞点：无。
- 下一步建议：请管理员按链路新规创建唯一后续审查任务，在同一 `worktree` 中只读复核 `kernel_gen/dsl/gen_kernel.py`、`test/dsl/test_gen_kernel.py` 与已合并 `spec/dsl/gen_kernel.md` / `ARCHITECTURE/plan/npu_demo_backend_plan.md` 是否完全一致，重点检查 `KernelContext` 首参、固定 body 顺序与禁止回退词。

时间：2026-04-02 07:18:17 +0800
任务：T-20260402-98b72544
任务目标：在 `wt-20260402-npu-i1` 只读复核 `kernel_gen/dsl/gen_kernel.py` 与 `test/dsl/test_gen_kernel.py` 是否已实现 `target="npu_demo"` 的 body-level kernel 骨架，并确认显式包含 `KernelContext`、`thread_id/thread_num`、`TSM/TLM dynamic memory`、`view/slice/deslice/add`，且不回退到 `.view/load/store/launch/barrier/arch.launch_kernel`。
改动：
- 只读核对：
  - `wt-20260402-npu-i1/kernel_gen/dsl/gen_kernel.py`
  - `wt-20260402-npu-i1/test/dsl/test_gen_kernel.py`
  - `wt-20260402-npu-i1/spec/dsl/gen_kernel.md`
  - `ARCHITECTURE/plan/npu_demo_backend_plan.md`
- 复核结果：
  - `kernel_gen/dsl/gen_kernel.py:337-377` 已按合同生成固定 `npu_demo` body-level kernel 骨架：`npu_demo::KernelContext& ctx`、`ctx.thread_id()`、`ctx.thread_num()`、`ctx.get_dynamic_memory<T>(MemorySpace::TSM/TLM)`、`view/slice/add/deslice` 顺序齐全。
  - `kernel_gen/dsl/gen_kernel.py:382-407` 已实现“当前冻结子集只接受空 body”的校验 helper；我额外用只读临时脚本复现，确认同名函数若 body 含 `test.unsupported` 会报 `unsupported npu_demo body-level kernel body op test.unsupported`，若 body 含 `func.return` 会报 `npu_demo body-level kernel body must match frozen subset`。
  - `test/dsl/test_gen_kernel.py:654-704` 目前只覆盖了正向骨架与禁止词检查，没有机械锁定上述两条负向边界。
- 验证：
  - `pytest -q test/dsl/test_gen_kernel.py -k 'test_gen_kernel_emits_npu_demo_body_level_kernel or test_gen_kernel_emits_npu_demo_memory_pipeline'`（exit `0`，`2 passed, 20 deselected`）
  - `pytest -q test/dsl/test_gen_kernel.py`（exit `0`，`22 passed`）
  - `git -C /home/lfr/kernelcode_generate/wt-20260402-npu-i1 diff --check -- kernel_gen/dsl/gen_kernel.py test/dsl/test_gen_kernel.py`（exit `0`）
  - 只读临时复现：构造 `target=npu_demo` 且 body 含 `test.unsupported` / `func.return` 的最小 `func.func`，`gen_kernel(...)` 均按预期抛出 `GenKernelError`。
- 问题列表：
  - `P1`：`test/dsl/test_gen_kernel.py:654-704` 缺少对 `kernel_gen/dsl/gen_kernel.py:382-407` 这条错误路径的负向测试。当前实现虽然正确拒绝非空 body / 未知 op，但测试只锁住了正向骨架与禁止词，未锁住“固定骨架不能静默吞掉非法 body”这一关键边界。该回归模式刚在 `conv2d_img2col2d_tiled` 链路真实出现过；如果未来重构误删 `_validate_npu_demo_body_level_kernel_body(...)` 或绕过它，现有测试仍会全部通过，无法及时发现函数级骨架吞错问题。
结论：
- `需修改`。
- 原因不是当前实现骨架错误，而是关键错误路径缺少测试锁定，存在与已发生过的同类回归相同的测试空洞。
- 修改建议：仅补 `test/dsl/test_gen_kernel.py` 的负向用例，至少覆盖：
  - `target=npu_demo` 同名函数 body 含未知 op 时必须报错；
  - `target=npu_demo` 同名函数 body 非空（如仅 `func.return`）时必须报错。
- 按管理员新规则，本任务完成后不自行新建后续任务；如需进入下一步，请由管理员创建并分发。

时间：2026-04-02 07:22:15 +0800
任务：T-20260402-dbc07781
任务目标：在 `wt-20260402-npu-i1` 中仅修改 `test/dsl/test_gen_kernel.py`，补 `target="npu_demo"` 的非空 body / 未知 op 负向边界测试，锁定 body-level kernel 骨架在非法 body 场景下必须拒绝生成；不改 `kernel_gen/dsl/gen_kernel.py`、`spec`、`emit_c`。
改动：
- 前置核对：
  - `worktree=/home/lfr/kernelcode_generate/wt-20260402-npu-i1` 可访问。
  - 主仓 `TODO.md` 显示当前我名下仅本任务进行中，无并行冲突。
  - 记录文件沿用同链路 `/home/lfr/kernelcode_generate/agents/codex-multi-agents/log/task_records/2026/14/20260402-npu-i1.md`。
- 只读确认缺口：
  - `kernel_gen/dsl/gen_kernel.py` 已在 `npu_demo` 路径中实现 `_validate_npu_demo_body_level_kernel_body(...)`，会拒绝未知 op 与非空 body。
  - `test/dsl/test_gen_kernel.py` 之前只锁正向骨架与禁止词，没有把上述两条错误路径机械固化为回归测试。
- 本轮仅修改 `wt-20260402-npu-i1/test/dsl/test_gen_kernel.py`：
  - 新增 `test_gen_kernel_rejects_npu_demo_body_level_kernel_with_unknown_body_op`，验证 `target=npu_demo` 且 body 含 `test.unsupported` 时，`gen_kernel(...)` 必须抛 `GenKernelError`，并带出 `test.unsupported`。
  - 新增 `test_gen_kernel_rejects_npu_demo_body_level_kernel_with_nonempty_body`，验证 `target=npu_demo` 且 body 非空（最小 `func.ReturnOp()`）时，`gen_kernel(...)` 必须报 `body must match frozen subset`。
- 验证：
  - `pytest -q /home/lfr/kernelcode_generate/wt-20260402-npu-i1/test/dsl/test_gen_kernel.py -k 'npu_demo'`（exit 0，`4 passed, 20 deselected in 0.26s`）
  - `pytest -q /home/lfr/kernelcode_generate/wt-20260402-npu-i1/test/dsl/test_gen_kernel.py`（exit 0，`24 passed in 0.36s`）
  - `git -C /home/lfr/kernelcode_generate/wt-20260402-npu-i1 diff --check -- test/dsl/test_gen_kernel.py`（exit 0）
  - `git -C /home/lfr/kernelcode_generate/wt-20260402-npu-i1 status --short -- kernel_gen/dsl/gen_kernel.py test/dsl/test_gen_kernel.py` 显示 `gen_kernel.py` 与 `test_gen_kernel.py` 仍在链路改动集中；其中本轮只新增 `test_gen_kernel.py` 的两条负向测试，`gen_kernel.py` 为前序 `T-20260402-f333980b` 已存在的同链路实现改动，本轮未触碰。
结论：
- 已完成本任务要求，影响范围仅 `wt-20260402-npu-i1/test/dsl/test_gen_kernel.py`。
- `npu_demo` 的 body-level kernel 现在同时具备正向骨架测试和非法 body 负向边界测试，能够机械锁定“固定骨架不能吞掉未知 op / 非空 body”。
- 阻塞点：无。
- 下一步建议：请管理员按链路创建唯一复审任务，在同一 `worktree` 中只读复核 `kernel_gen/dsl/gen_kernel.py` 与 `test/dsl/test_gen_kernel.py` 的 `npu_demo` 骨架及负向边界是否闭环。

时间：2026-04-02 08:17:47 +0800
任务：T-20260402-deb601bf
任务目标：在 `wt-20260402-npu-i1` 中只读复核 `test/dsl/test_gen_kernel.py` 是否已补 `target="npu_demo"` 的非空 body / 未知 op 负向边界测试，并确认 body-level kernel 骨架与禁止项保持不变。
改动：
- 静态对照：
  - `wt-20260402-npu-i1/kernel_gen/dsl/gen_kernel.py`
  - `wt-20260402-npu-i1/test/dsl/test_gen_kernel.py`
  - `wt-20260402-npu-i1/spec/dsl/gen_kernel.md`
  - `ARCHITECTURE/plan/npu_demo_backend_plan.md`
- 复核结果：
  - `test/dsl/test_gen_kernel.py` 第 654-704 行保留了正向骨架测试，继续锁定 `KernelContext` 首参、`thread_id/thread_num`、`TSM/TLM` dynamic memory、`view/slice/add/deslice` 固定顺序，以及 `.view<` / `load<` / `store<` / `slice(source` / `arch.launch_kernel` 禁止项。
  - `test/dsl/test_gen_kernel.py` 第 718-727 行已补 `test_gen_kernel_rejects_npu_demo_body_level_kernel_with_unknown_body_op`，覆盖 body 含 `test.unsupported` 时必须报错。
  - `test/dsl/test_gen_kernel.py` 第 741-748 行已补 `test_gen_kernel_rejects_npu_demo_body_level_kernel_with_nonempty_body`，覆盖 body 非空但只含 `func.return` 时必须报 `body must match frozen subset`。
  - `kernel_gen/dsl/gen_kernel.py` 第 382-407 行的 `_validate_npu_demo_body_level_kernel_body(...)` 与上述两条测试保持一致，正向骨架仍由第 337-379 行固定生成，未见回退到 `launch/barrier/arch.launch_kernel` 或 `.view/load/store`。
- helper import 新边界核对：
  - 本任务范围只涉及 `gen_kernel` 的函数级源码拼装测试，不涉及 DSL/AST helper 名解析入口；未发现对“未导入裸 helper 名也按 helper 解析”的依赖或放宽。
- 验证：
  - `pytest -q test/dsl/test_gen_kernel.py -k 'npu_demo'`（exit 0，`4 passed, 20 deselected in 0.40s`）
  - `git diff -- kernel_gen/dsl/gen_kernel.py test/dsl/test_gen_kernel.py`
结论：
- `通过`。
- 漏洞排查结果：
  - 未发现 `npu_demo` 固定骨架吞掉未知 op / 非空 body 的测试缺口残留。
  - 未发现 body-level kernel 骨架与禁止项回退。
  - 未发现本任务范围内 helper import 新边界被破坏。
- 按管理员新规则，本任务完成后不自行新建后续任务；如需继续沿链路推进，请由管理员创建并分发。
时间：2026-04-02 09:12:05 +0800
任务：T-20260402-551045d7
任务目标：在 `wt-20260402-npu-i1` 按最小范围合入 `kernel_gen/dsl/gen_kernel.py`、`test/dsl/test_gen_kernel.py` 与同链路记录文件；不改 `spec/emit_c`；合并后完成 cleanup 与状态同步。
改动：
- 合并范围确认：
  - `git -C wt-20260402-npu-i1 status --short -- kernel_gen/dsl/gen_kernel.py test/dsl/test_gen_kernel.py` 显示仅 `kernel_gen/dsl/gen_kernel.py` 与 `test/dsl/test_gen_kernel.py` 为业务改动；链路记录文件沿主仓路径维护。
  - 最新复审结论来自 `T-20260402-deb601bf`，状态为 `通过`，阻塞已解除。
- 合入主仓内容：
  - 在主仓现有 `nn.add` direct-return 与 `conv2d_img2col2d_tiled` 骨架之上，最小并入 `npu_demo` body-level kernel：
    - 新增 `_is_npu_demo_body_level_kernel(...)`
    - 新增 `_get_npu_demo_body_level_kernel_types(...)`
    - 新增 `_emit_npu_demo_body_level_kernel_body(...)`
    - 新增 `_validate_npu_demo_body_level_kernel_body(...)`
    - 在 `gen_signature(...)` 中为 `target="npu_demo"` 特判签名：`npu_demo::KernelContext& ctx + source -> out`
    - 在 `gen_body(...)` 中为 `target="npu_demo"` 特判固定 body-level kernel 骨架
  - 在 `test/dsl/test_gen_kernel.py` 追加 `GK-017~020`，覆盖：
    - `KernelContext` 首参与 `thread_id/thread_num`
    - `TSM/TLM dynamic memory` 与 `view/slice/add/deslice` 固定顺序
    - unknown body op 必须报错
    - nonempty body 必须报错
  - 由于授权 `worktree` 基线早于主仓已合入的 `nn.add` / `conv` 链路，本次未整文件覆盖，而是在主仓现状上手工并入 `npu_demo` 固定骨架与负向边界，保留了既有 `nn.add` / `conv` 逻辑与测试。
- 验证：
  - `pytest -q test/dsl/test_gen_kernel.py`（exit 0，`25 passed in 0.58s`）
  - `git diff --check -- kernel_gen/dsl/gen_kernel.py test/dsl/test_gen_kernel.py`（exit 0）
- cleanup：
  - `git worktree remove --force wt-20260402-npu-i1`（exit 0）
  - `git branch -D wt-20260402-npu-i1`（exit 0）
  - `git worktree list --porcelain` 复核后，授权 `worktree` 已移除，且未波及其他活跃 worktree。
结论：
- 已完成合并收口；本次仅合入 `kernel_gen/dsl/gen_kernel.py`、`test/dsl/test_gen_kernel.py` 与当前链路记录文件，未改 `spec/emit_c`。
- 测试情况：`test/dsl/test_gen_kernel.py` 全量通过，结果 `25 passed`。
- 阻塞点：无。
- 下一步建议：该链路已收口，无需继续派生后续任务；如需后续调整 `spec/emit_c` 或 AST/helper 边界，应由管理员单独分发新任务。
