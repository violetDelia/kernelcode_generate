时间：2026-04-02 03:15:02 +0800
任务：T-20260402-b19a581d
任务目标：在 `spec/target/registry.md` 冻结 `npu_demo` 的固定 target 模板与能力矩阵；仅修改该 spec 文件，不改实现、测试、其他 spec。
改动：
- 核对任务前置：
  - `worktree=wt-20260402-npu-n1` 可访问。
  - `wt-20260402-npu-n1/skills/codex-multi-agents/scripts/` 下的 `codex-multi-agents-task.sh`、`codex-multi-agents-tmux.sh`、`codex-multi-agents-list.sh` 存在，但未加入 `PATH`，后续需按相对路径调用。
  - `wt-20260402-npu-n1/agents/codex-multi-agents/log/task_records/2026/14/` 目录存在；本记录文件原先不存在，现按约定创建。
  - 当前会话未收到其他新任务；但 `wt-20260402-npu-n1/TODO.md` 缺失，无法按规范执行 `codex-multi-agents-task.sh -status -doing` 核验 doing 列表，也无法在完成后执行 `-done/-new`。
- 已读取 `spec/target/registry.md` 当前基线，尚未开始正文修改。
结论：
- 已开工。
- 当前阻塞：`TODO.md` 缺失，影响任务状态核验及后续 `-done/-new` 流程；需管理员确认任务文件位置或补齐后才能继续。
时间：2026-04-02 03:15:02 +0800
任务：T-20260402-b19a581d
任务目标：在 `spec/target/registry.md` 冻结 `npu_demo` 的固定 target 模板与能力矩阵；仅修改该 spec 文件，不改实现、测试、其他 spec。
改动：
- 管理员已确认任务状态流转统一使用主仓 `TODO.md`：`/home/lfr/kernelcode_generate/TODO.md`。
- 已用主仓任务表执行 `skills/codex-multi-agents/scripts/codex-multi-agents-task.sh -file TODO.md -status -doing`，确认当前我名下仅 `T-20260402-b19a581d` 在进行中。
- 阻塞已解除，继续按 `N1` 边界推进 `wt-20260402-npu-n1/spec/target/registry.md`。
结论：
- 当前无流程阻塞。
- 下一步：只修改 `spec/target/registry.md`，补齐 `npu_demo` 的固定硬件模板、已启用能力与固定不支持项。
时间：2026-04-02 03:19:49 +0800
任务：T-20260402-b19a581d
任务目标：在 `spec/target/registry.md` 冻结 `npu_demo` 的固定 target 模板与能力矩阵；仅修改该 spec 文件，不改实现、测试、其他 spec。
改动：
- 更新 `spec/target/registry.md`：
  - 将来源说明从“仅 `json/txt`”扩展为“固定内置模板 + `json/txt`”，明确 `npu_demo` 可作为内置固定模板收敛。
  - 在 `目标`、`限制与边界` 中补充显式白名单 target 的固定判定规则，写明白名单外能力不得默认放开，尤其不能把 `arch.launch_kernel`、`launch`、`barrier` 视为已启用。
  - 在 `配置格式` 下新增“固定内置模板”小节，以 `TargetSpec(...)` 形式写死 `npu_demo` 固定模板：
    - `block_num=6`
    - `thread_num=8`
    - `subthread_num=1`
    - `sm_memory_size=0`
    - `lm_memory_size=0`
    - `tsm_memory_size=24576`
    - `tlm_memory_size=2048`
    - 已启用能力仅收敛为 `arch.get_block_id/get_block_num/get_thread_id/get_thread_num/get_subthread_id/get_subthread_num/get_dynamic_memory`
    - `arch.launch_kernel` 不在已启用能力中；`launch/barrier` 相关能力查询固定未启用
  - 在“与 arch 层的联动约束”与“测试清单”中补入 `npu_demo` 固定硬件值与未支持能力的读取/验收口径，并写明下游专项测试名：
    - `test_target_registry_npu_demo_template`
    - `test_target_registry_npu_demo_rejects_unsupported_ops`
- 自检：
  - `rg -n "npu_demo|block_num|thread_num|subthread_num|tsm_memory_size|tlm_memory_size|barrier|launch" spec/target/registry.md`（exit 0）
  - `git diff --check -- spec/target/registry.md`（exit 0）
结论：
- 已完成本任务要求，改动范围仅 `wt-20260402-npu-n1/spec/target/registry.md` 与同链路记录文件。
- 测试情况：本任务为 spec 阶段，未运行 `pytest`；已完成任务要求的 grep 校验与 diff 格式校验。
- 下一步建议：新建唯一后续任务，进入 `N2 spec任务`，仅修改 `spec/dsl/emit_c.md`，冻结 `target="npu_demo"` 的节点级文本映射，明确 `KernelContext`、`TSM/TLM dynamic memory`、`view/slice/deslice/add` 的发射规则，并继续沿用 `worktree=wt-20260402-npu-n1` 与本记录文件。
时间：2026-04-02 03:24:40 +0800
任务：T-20260402-0a8e5121
任务目标：审查 `spec/target/registry.md` 是否与 `ARCHITECTURE/plan/npu_demo_backend_plan.md` 的 `N1` 目标、边界、注意事项、验证命令、验收标准一致；重点核对 `npu_demo` 固定模板值 `block_num=6`、`thread_num=8`、`subthread_num=1`、`sm/lm=0`、`tsm=24576`、`tlm=2048`，以及 `launch/barrier/arch.launch_kernel` 固定未启用。
改动：
- 只读审查范围：
  - `spec/target/registry.md`
  - `test/target/test_target_registry.py`
  - `kernel_gen/target/registry.py`
  - `/home/lfr/kernelcode_generate/ARCHITECTURE/plan/npu_demo_backend_plan.md`
- 复核命令：
  - `git -C /home/lfr/kernelcode_generate/wt-20260402-npu-n1 diff -- spec/target/registry.md`（exit 0）
  - `nl -ba spec/target/registry.md | sed -n '1,280p'`（exit 0）
  - `nl -ba test/target/test_target_registry.py | sed -n '1,260p'`（exit 0）
  - `rg -n "npu_demo|test_target_registry_npu_demo_template|test_target_registry_npu_demo_rejects_unsupported_ops|launch|barrier|arch.launch_kernel" test/target/test_target_registry.py kernel_gen/target/registry.py`（exit 1）
- 审查发现：
  - 问题 1：`spec/target/registry.md` 已声明支持三类来源并新增 `npu_demo` 固定内置模板（见 `spec/target/registry.md:6`、`spec/target/registry.md:108-144`），但同文件公开接口 `load_targets(directory: Path)` 仍只描述“扫描目录并加载 *.json/*.txt target 文件”（见 `spec/target/registry.md:172-186`），未说明内置模板何时注册、是否随模块初始化/`load_targets` 自动可用。风险：`N1` 验收中的 `test_target_registry_npu_demo_template` 无法从 spec 推导出可复现入口，调用方不知道应直接查询、先 `load_targets(default_dir)` 还是需要显式注册。
  - 问题 2：`spec/target/registry.md` 已把 `test_target_registry_npu_demo_template` 与 `test_target_registry_npu_demo_rejects_unsupported_ops` 写入测试清单（见 `spec/target/registry.md:260-261`），但当前 `test/target/test_target_registry.py` 全文件 319 行内不存在任何 `npu_demo`/`launch`/`barrier` 相关测试名（`wc -l` 与 `tail -n 80` 已复核；对应 `rg` 退出码 1）。按审查规范，“测试目标与可执行测试可映射”是硬门槛，当前 spec/test 已脱节。
  - 问题 3：`spec/target/registry.md` 的公开接口 `is_arch_op_supported(target, op_name)` 明确作用对象是 `arch.*` op（见 `spec/target/registry.md:194-199`），但测试清单与模板说明同时要求对 `launch` / `barrier` 做能力查询（见 `spec/target/registry.md:143`、`spec/target/registry.md:218`、`spec/target/registry.md:254`、`spec/target/registry.md:261`）。风险：`launch`/`barrier` 是否作为合法 `op_name` 输入没有被正式定义，验收口径存在接口域歧义。
- 漏洞/歧义排查：
  - 输入校验绕过：`npu_demo` 白名单与 `arch.launch_kernel` 黑名单值本身与 plan 一致，但内置模板注册入口未定义，容易导致调用方绕开模板直接落到“unknown target”分支。
  - 类型/形状绕过：本任务不涉及类型/shape。
  - 边界越界：不涉及内存边界，但能力查询输入域（`arch.*` vs `launch/barrier`）边界不清。
  - 错误处理缺失：未明确 `npu_demo` 固定模板何时存在，会让“未注册 target”与“能力未启用”两类错误混淆。
  - 状态污染：未发现。
  - 资源释放问题：不涉及。
结论：
- 需修改。
- 建议修正：
  - 在 `spec/target/registry.md` 的 `load_targets(...)` / 固定模板章节明确 `npu_demo` 内置模板的注册时机和查询入口，例如“模块初始化即注册”或“`load_targets(...)` 后保证存在”，使 `N1` 验收可复现。
  - 在测试尚未落地前，不要把 `test_target_registry_npu_demo_template` / `test_target_registry_npu_demo_rejects_unsupported_ops` 写成当前测试清单映射；可改为“下游待补测试/验收标准”，或同步补齐 `test/target/test_target_registry.py` 后再映射。
 - 对 `launch` / `barrier`，要么在 spec 中明确它们可作为 capability key 传给查询接口，要么把 `N1` 验收限定为 `arch.*` 域内的固定未启用项，避免接口域歧义。
时间：2026-04-02 03:30:39 +0800
任务：T-20260402-658e2245
任务目标：在 `N1` 修正 spec 阶段仅修改 `spec/target/registry.md`，明确 `npu_demo` 固定内置模板的注册/查询入口，处理两个未落地测试名导致的 spec/test 映射冲突，并收敛 `launch/barrier` 能力查询的接口域表述。
改动：
- 更新 `spec/target/registry.md`：
  - 将功能简介收敛为“固定内置模板 + 目录 target”的统一注册/查询入口，而不再只强调配置文件加载。
  - 在限制与边界中明确 `is_arch_op_supported(...)` 的稳定输入域限定为 `arch.*` 能力键；`launch`、`barrier` 仅作为上层能力语义，不单独构成 registry 公共查询接口输入。
  - 在 `npu_demo` 固定内置模板小节中明确：
    - 标准注册入口是 registry 的固定内置模板注册流程，而不是外部 `json/txt` 文件。
    - 标准查询入口是 `is_arch_op_supported(...)`、`get_target_hardware(...)` 与 `get_current_target_hardware(...)`。
    - `arch.launch_kernel` 通过 `is_arch_op_supported("npu_demo", "arch.launch_kernel")` 查询必须未启用；`launch/barrier` 仅作为上层能力语义，其最终能力结论也必须收敛为未启用。
  - 在 `load_targets(...)`、`register_target(...)`、`is_arch_op_supported(...)` 的公开接口说明中补齐内置模板与查询域约束：`load_targets(...)` 不是 `npu_demo` 的唯一注册入口，`register_target(...)` 不要求调用方手工注册固定内置模板，`is_arch_op_supported(...)` 不接受裸 `launch/barrier` 作为标准输入。
  - 在测试清单中把 `test_target_registry_npu_demo_template` 与 `test_target_registry_npu_demo_rejects_unsupported_ops` 收回为“下游待补验收标准”，并明确当前 `test/target/test_target_registry.py` 尚未落地这两个用例，因此不再把它们写成当前已映射测试。
- 验证：
  - `rg -n "npu_demo|register|load_targets|is_arch_op_supported|get_target_hardware|get_current_target_hardware|launch|barrier|test_target_registry_npu_demo_template|test_target_registry_npu_demo_rejects_unsupported_ops" spec/target/registry.md`（exit 0）
  - `git diff --check -- spec/target/registry.md agents/codex-multi-agents/log/task_records/2026/14/20260402-npu-demo-n1.md`（exit 0）
结论：
- `完成`。
- 影响范围仅 `spec/target/registry.md` 与当前链路记录文件；未修改测试、计划或实现文件。
- 测试情况：本任务为 spec-only，未运行 `pytest`。
- 下一步建议：新建唯一后续任务，进入 `N1` 修正 spec 的复审阶段，在同一 `worktree` 与记录文件中只读复核内置模板入口、接口域与待补验收标准的口径是否已闭环，不提前进入 `N2` 或实现阶段。
时间：2026-04-02 03:36:18 +0800
任务：T-20260402-ab8393dc
任务目标：只读复核 `spec/target/registry.md` 是否已明确 `npu_demo` 固定内置模板的注册/查询入口，是否已把 `launch/barrier` 收敛为接口域外的上层能力语义，并确认 `test_target_registry_npu_demo_template` 与 `test_target_registry_npu_demo_rejects_unsupported_ops` 已收回为下游待补验收标准。
改动：
- 只读复审范围：
  - `spec/target/registry.md`
  - `test/target/test_target_registry.py`
  - `kernel_gen/target/registry.py`
  - `/home/lfr/kernelcode_generate/ARCHITECTURE/plan/npu_demo_backend_plan.md`
- 复核命令：
  - `nl -ba /home/lfr/kernelcode_generate/wt-20260402-npu-n1/spec/target/registry.md | sed -n '1,290p'`（exit 0）
  - `rg -n "test_target_registry_npu_demo_template|test_target_registry_npu_demo_rejects_unsupported_ops|npu_demo|launch|barrier|arch.launch_kernel|is_arch_op_supported|load_targets|register_target|get_target_hardware|get_current_target_hardware" /home/lfr/kernelcode_generate/wt-20260402-npu-n1/spec/target/registry.md /home/lfr/kernelcode_generate/wt-20260402-npu-n1/test/target/test_target_registry.py /home/lfr/kernelcode_generate/wt-20260402-npu-n1/kernel_gen/target/registry.py`（exit 0）
- 复审结果：
  - `spec/target/registry.md:146`、`spec/target/registry.md:180`、`spec/target/registry.md:196` 已明确 `npu_demo` 的标准注册入口是 registry 固定内置模板注册流程，而非外部 `json/txt` 文件；标准查询入口收敛为 `is_arch_op_supported(...)`、`get_target_hardware(...)` 与 `get_current_target_hardware(...)`。这次已补上前次审查指出的“内置模板何时可查”口径缺口。
  - `spec/target/registry.md:37`、`spec/target/registry.md:144`、`spec/target/registry.md:203`、`spec/target/registry.md:223` 已把 `launch/barrier` 收敛为接口域外的上层能力语义：`is_arch_op_supported(...)` 的稳定输入域限定为 `arch.*`，`launch/barrier` 不再被写成 registry 公共查询接口的标准输入。
  - `spec/target/registry.md:265-266` 已将 `test_target_registry_npu_demo_template` 与 `test_target_registry_npu_demo_rejects_unsupported_ops` 收回为“下游待补验收标准”，并明确当前 `test/target/test_target_registry.py` 尚未落地这两个用例，因此不再视为已映射的现有测试清单；这与当前测试文件实际状态一致。
- 漏洞/歧义排查：
  - 输入校验绕过：`npu_demo` 白名单与 `arch.launch_kernel` 未启用口径保持收敛，未重新放开未支持能力。
  - 类型/形状绕过：本任务不涉及。
  - 边界越界：接口域边界已收敛到 `arch.*`，`launch/barrier` 不再混入稳定查询接口。
  - 错误处理缺失：未发现新的“unknown target”与“能力未启用”混淆表述。
  - 状态污染：未发现。
  - 资源释放问题：不涉及。
结论：
- 通过。
- 未发现额外改进点。
- 测试情况：本次为复审阶段，未执行 `pytest`；证据来自 plan/spec/test/实现接口说明的静态对照与行号复核。
- 下一步建议：按链路进入 `N2` spec 任务，仅修改 `spec/dsl/emit_c.md`，冻结 `target="npu_demo"` 的节点级文本映射。
