时间：2026-04-02 04:26:44 +0800
任务：T-20260402-d4426a40
任务目标：按 `ARCHITECTURE/plan/nn_add_cpu_e2e_plan.md` 的 `S3`，仅修改 `spec/dsl/gen_kernel.md`，冻结 direct-return `nn.add -> cpu::add(..., out)` 的函数级合同。
改动：
- 核对任务前置：
  - `worktree=/home/lfr/kernelcode_generate/wt-20260402-nn-add-s3` 可访问。
  - 当前无其他由睡觉小分队进行中的任务，已按要求向管理员同步开工状态。
- 更新 `spec/dsl/gen_kernel.md`：
  - 在 `目标` 与 `限制与边界` 中新增 direct-return `nn.add` 的函数级 CPU 合同，明确仅当 `nn.add.result` 的唯一 use 为 `func.return` 且结果可直接绑定到函数 `out` 时，允许特化为 `cpu::add(..., out);`。
  - 在 `gen_signature(...)` 注意事项中写清签名形态：direct-return `nn.add` 仍保持 `Memory<...>& out` 输出参数形式，`memory + symbol.int` 场景的标量参数应生成为 `long long bias` 一类 CPU 标量参数，并补充完整目标源码示例。
  - 在 `gen_body(...)` 注意事项中写清函数体形态：允许的三条 direct-return 成功语句为 `cpu::add(lhs, rhs, out);`、`cpu::add(lhs, 1, out);`、`cpu::add(lhs, bias, out);`。
  - 明确 `unique-use + func.return + direct bind to out` 是 direct-return `nn.add` 特化的硬门禁；multi-use 或无法直接绑定 `out` 时继续保持 `EmitCError target=cpu: nn.add: unsupported op`，不得退化为 `out = tmp` 或 `return tmp`。
  - 在测试目标中补充 direct-return `nn.add` 的函数级验证范围，但未修改测试文件。
- 自检：
  - `rg -n "nn.add|cpu::add\\(.*out\\)|unique-use|func.return|unsupported op" spec/dsl/gen_kernel.md`（exit 0）
  - `git diff --check -- spec/dsl/gen_kernel.md`（exit 0）
结论：
- 已完成本任务要求，变更范围仅 `wt-20260402-nn-add-s3/spec/dsl/gen_kernel.md` 与本记录文件。
- 测试情况：本任务为 spec 阶段，未运行 `pytest`；已完成关键词校验与 diff 格式校验。
- 下一步建议：创建唯一后续实现任务，在同一 `worktree` 中对齐 `kernel_gen/dsl/gen_kernel.py` 与 `test/dsl/test_gen_kernel.py`，按当前 `S3` 合同实现 direct-return `nn.add` 的函数级 CPU 收口。
时间：2026-04-02 04:29:57 +0800
任务：T-20260402-7c435987
任务目标：修正 `S3` spec；仅修改 `spec/dsl/gen_kernel.md`，补齐 direct-return `nn.add` 四个验收用例与计划 `S3` 的映射，或明确降级为下游待补测试映射，并保持 `unique-use + func.return + direct bind to out` 的硬门禁与 `unsupported op` 边界不变。
改动：
- 核对任务前置：
  - `worktree=/home/lfr/kernelcode_generate/wt-20260402-nn-add-s3` 可访问。
  - 当前无其他由睡觉小分队进行中的任务，已向管理员同步“已开始、无阻塞、仅修改 spec/dsl/gen_kernel.md”。
- 更新 `spec/dsl/gen_kernel.md`：
  - 在 `测试目标` 中新增说明：`S3` 计划中的四个 direct-return `nn.add` 验收用例当前冻结为本 spec 的下游待补测试映射，在 `test/dsl/test_gen_kernel.py` 实际落地前，不冒充为当前已存在的可执行测试。
  - 在 `功能与用例清单` 中补齐四个 direct-return `nn.add` 验收项：
    - `GK-013`：`memory + memory` -> `cpu::add(lhs, rhs, out);`
    - `GK-014`：`memory + const(i32)` -> `cpu::add(lhs, 1, out);`
    - `GK-015`：`memory + symbol.int` -> `long long bias` + `cpu::add(lhs, bias, out);`
    - `GK-016`：multi-use 或无法 direct bind `out` 时继续报 `EmitCError target=cpu: nn.add: unsupported op`
  - 上述四项均明确标注为“下游待补测试映射”，与计划 `S3` 的四个验收测试名一一对应，同时不改动正文中 `unique-use + func.return + direct bind to out` 的硬门禁与 `unsupported op` 失败边界。
- 自检：
  - `rg -n "nn.add|cpu::add\\(.*out\\)|unique-use|func.return|unsupported op|test_gen_kernel_supports_direct_return_nn_add_memory_memory_on_cpu|test_gen_kernel_supports_direct_return_nn_add_memory_const_on_cpu|test_gen_kernel_supports_direct_return_nn_add_memory_symbol_on_cpu|test_gen_kernel_rejects_nn_add_specialization_on_multi_use|下游待补测试映射" spec/dsl/gen_kernel.md`（exit 0）
  - `git diff --check -- spec/dsl/gen_kernel.md`（exit 0）
结论：
- 已完成本任务要求，变更范围仅 `wt-20260402-nn-add-s3/spec/dsl/gen_kernel.md` 与同链路记录文件。
- 测试情况：本任务为 spec 修正阶段，未运行 `pytest`；已完成关键词校验与 diff 格式校验。
- 下一步建议：创建唯一后续实现任务，在同一 `worktree` 中按修正后的 `S3` spec 对齐 `kernel_gen/dsl/gen_kernel.py` 与 `test/dsl/test_gen_kernel.py`，补齐四个 direct-return `nn.add` 用例与函数级 CPU 收口实现。
时间：2026-04-02 04:24:30 +0800
任务：T-20260402-7c61ed67
任务目标：审查 `spec/dsl/gen_kernel.md` 是否与 `ARCHITECTURE/plan/nn_add_cpu_e2e_plan.md` 的 `S3` 目标、边界、注意事项、验证命令、验收标准一致；重点检查 direct-return `nn.add -> cpu::add(..., out)` 的函数级合同是否已写清 `unique-use + func.return + direct bind to out` 的硬门禁，并确认 multi-use 或无法直接绑定 `out` 时继续保持 `unsupported op`。
改动：
- 只读审查范围：
  - `spec/dsl/gen_kernel.md`
  - `test/dsl/test_gen_kernel.py`
  - `/home/lfr/kernelcode_generate/ARCHITECTURE/plan/nn_add_cpu_e2e_plan.md`
- 复核命令：
  - `git -C /home/lfr/kernelcode_generate/wt-20260402-nn-add-s3 diff -- spec/dsl/gen_kernel.md`（exit 0）
  - `nl -ba /home/lfr/kernelcode_generate/wt-20260402-nn-add-s3/spec/dsl/gen_kernel.md | sed -n '20,190p'`（exit 0）
  - `rg -n "nn.add|cpu::add\\(.*out\\)|unique-use|func.return|unsupported op|direct-return|Memory<int32_t>& out|long long bias|out = tmp|EmitCError" /home/lfr/kernelcode_generate/wt-20260402-nn-add-s3/spec/dsl/gen_kernel.md /home/lfr/kernelcode_generate/wt-20260402-nn-add-s3/test/dsl/test_gen_kernel.py`（exit 0）
  - `rg -n "test_gen_kernel_supports_direct_return_nn_add_memory_memory_on_cpu|test_gen_kernel_supports_direct_return_nn_add_memory_const_on_cpu|test_gen_kernel_supports_direct_return_nn_add_memory_symbol_on_cpu|test_gen_kernel_rejects_nn_add_specialization_on_multi_use" /home/lfr/kernelcode_generate/wt-20260402-nn-add-s3/test/dsl/test_gen_kernel.py`（exit 1）
- 审查发现：
  - 通过项：`spec/dsl/gen_kernel.md:30`、`:41`、`:99-105`、`:142-152` 已把 direct-return `nn.add -> cpu::add(..., out)` 的函数级合同写清，明确了 `unique-use + func.return + direct bind to out` 的硬门禁，并要求 multi-use 或无法直接绑定 `out` 时继续保持 `EmitCError target=cpu: nn.add: unsupported op`，未退化为 `out = tmp` / `return tmp`。
  - 问题 1：`ARCHITECTURE/plan/nn_add_cpu_e2e_plan.md:326-337` 把 `test_gen_kernel_supports_direct_return_nn_add_memory_memory_on_cpu`、`test_gen_kernel_supports_direct_return_nn_add_memory_const_on_cpu`、`test_gen_kernel_supports_direct_return_nn_add_memory_symbol_on_cpu`、`test_gen_kernel_rejects_nn_add_specialization_on_multi_use` 四个测试名写成了 `S3` 的明确验收标准；但 `spec/dsl/gen_kernel.md:177-190` 的“功能与用例清单”仍停在 `GK-012`，没有把这四个用例映射进去。按审查规范，“测试目标与可执行测试可映射”是硬门槛，当前 spec 与 plan 的验收映射未闭环。
  - 问题 2：当前 `test/dsl/test_gen_kernel.py` 中仍不存在上述四个测试名（对应 `rg` 退出码 1），而 `spec/dsl/gen_kernel.md` 也没有像 `S2` 修正那样把它们显式降级为“下游待补测试映射/验收标准”。风险：读 spec 的人会以为验收已经可以直接映射到现有测试，但实际上测试尚未落地，验收口径仍不够可复现。
- 漏洞/歧义排查：
  - 输入校验绕过：函数级特化的输入域已限制在 `memory + memory`、`memory + const(i32)`、`memory + symbol.int`，未放宽到范围外形态。
  - 类型/形状绕过：`memory + symbol.int -> long long bias` 与 `Memory<...>& out` 的签名口径已写清。
  - 边界越界：multi-use / 无法 direct bind out 的失败边界已保留。
  - 错误处理缺失：`unsupported op` 的失败口径已写清，但测试映射未闭环会让该错误边界缺少对应验收锚点。
  - 状态污染：未发现。
  - 资源释放问题：不涉及。
结论：
- 需修改。
- 建议修正：
  - 在 `spec/dsl/gen_kernel.md` 的“功能与用例清单”中显式加入这四个 direct-return `nn.add` 用例，与 plan 的 `S3` 验收标准一一对应；若当前 spec 阶段不想冒充已有测试，则至少应像 `S2` 一样把它们明确标记为“下游待补测试映射/验收标准”，避免映射悬空。
  - 保持现有正文合同不变，不要动 `unique-use + func.return + direct bind to out` 的硬门禁与 `unsupported op` 的失败边界。
时间：2026-04-02 04:33:19 +0800
任务：T-20260402-c36f1c9b
任务目标：只读复核 `spec/dsl/gen_kernel.md` 是否已补齐 direct-return `nn.add` 四个验收用例与计划 `S3` 的映射，或已明确降级为下游待补测试映射，并确认 `unique-use + func.return + direct bind to out` 的硬门禁与 `unsupported op` 边界保持不变；不改实现、测试或 expectation。
改动：
- 只读复核范围：
  - `spec/dsl/gen_kernel.md`
  - `test/dsl/test_gen_kernel.py`
  - `ARCHITECTURE/plan/nn_add_cpu_e2e_plan.md`
  - 当前链路记录文件
- 复核命令：
  - `rg -n "test_gen_kernel_supports_direct_return_nn_add_memory_memory_on_cpu|test_gen_kernel_supports_direct_return_nn_add_memory_const_on_cpu|test_gen_kernel_supports_direct_return_nn_add_memory_symbol_on_cpu|test_gen_kernel_rejects_nn_add_specialization_on_multi_use" test/dsl/test_gen_kernel.py`（exit 1）
  - `pytest -q test/dsl/test_gen_kernel.py -k 'direct_return_nn_add'`（exit 5，`12 deselected in 0.27s`）
  - `git diff --check -- spec/dsl/gen_kernel.md`（exit 0）
- 问题列表：
  - 本次复审未发现剩余必须修改项。
- 漏洞排查结果：
  - 输入校验绕过：正文仍把 direct-return `nn.add` 限定在 `unique-use + func.return + direct bind to out`，未放宽输入域。
  - 类型/形状绕过：`memory + memory`、`memory + const(i32)`、`memory + symbol.int` 三条函数体形态与 `long long bias` 口径保持不变。
  - 边界越界：multi-use / 无法 direct bind `out` 仍保持 `unsupported op`，未退化为 generic fallback。
  - 错误处理缺失：`EmitCError target=cpu: nn.add: unsupported op` 的失败边界仍在，未被弱化。
  - 状态污染：spec-only 复核，未见状态污染问题。
  - 资源释放问题：spec-only 复核，未涉及资源生命周期变更。
- 改进建议：
  - 未发现额外改进点。
- 残余风险：
  - 计划 `S3` 的四个 direct-return `nn.add` 测试名在 `test/dsl/test_gen_kernel.py` 中仍未落地，但当前 spec 已在 `GK-013` ~ `GK-016` 中把它们明确标注为“下游待补测试映射”，未再冒充为现有测试映射。
结论：
- `通过`。
- 复审结果：
  - `GK-013` ~ `GK-016` 已将计划 `S3` 的四个验收用例与 spec 一一对齐，并明确降级为下游待补测试映射。
  - `unique-use + func.return + direct bind to out` 的硬门禁与 `EmitCError target=cpu: nn.add: unsupported op` 的失败边界保持不变。
- 下一步建议：新建唯一后续任务，进入合并阶段，仅合入 `wt-20260402-nn-add-s3/spec/dsl/gen_kernel.md` 与当前链路记录文件。
时间：2026-04-02 05:22:59 +0800
任务：T-20260402-f62e301e
任务目标：将 `wt-20260402-nn-add-s3` 中已通过复审的 `S3` spec 成果按最小范围合入主分支，仅包含 `spec/dsl/gen_kernel.md` 与同链路记录文件；完成单次同步、cleanup 与状态封板。
改动：
- 核对合并边界：`TODO.md` 中当前 `worktree=wt-20260402-nn-add-s3` 仅存在本任务 `T-20260402-f62e301e`；`git -C wt-20260402-nn-add-s3 status --short` 仅见 `spec/dsl/gen_kernel.md` 变更，未发现范围外业务文件。
- 复核主分支差异：`spec/dsl/gen_kernel.md` 自 `wt-20260402-nn-add-s3` 基线以来已包含 `conv C2` 的函数级骨架章节；本次未整文件覆盖，而是在主仓 `gen_kernel.md` 上最小人工合入 `nn.add S3` 的 direct-return 规则，保留了现有 `conv C2` 正文。
- 复核 `git diff --check -- spec/dsl/gen_kernel.md agents/codex-multi-agents/log/task_records/2026/14/20260402-nn-add-cpu-s3.md` 通过；生成合并提交 `7d98a1e`（`T-20260402-f62e301e-merge-nn-add-s3`），提交内容仅包含 `spec/dsl/gen_kernel.md` 与 `agents/codex-multi-agents/log/task_records/2026/14/20260402-nn-add-cpu-s3.md`。
- 执行 cleanup：`git worktree remove --force wt-20260402-nn-add-s3` 与 `git branch -D wt-20260402-nn-add-s3`（均 exit 0）；清理后 `git worktree list --porcelain` 不再包含 `wt-20260402-nn-add-s3`，其余现存 worktree 保持原样未触碰。
- 未新增本轮测试；本次合并直接引用链路内最近一次审查/复审结论，证据来自 `ARCHITECTURE/plan/nn_add_cpu_e2e_plan.md`、`spec/dsl/gen_kernel.md` 与 `test/dsl/test_gen_kernel.py` 的静态对照。
结论：
- 完成。`S3` spec 已按限定范围合入主分支，对应 worktree/branch 已清理，无范围外文件混入。
- 本任务未扩到实现、测试或其他 spec；下一步建议由管理员按计划单独创建后续任务。
