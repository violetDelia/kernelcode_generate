时间：2026-04-02 06:07:13 +0800
任务：T-20260402-0a69ba66
任务目标：在 `wt-20260402-npu-impl-e2` 中仅修改 `kernel_gen/dsl/emit_c.py` 与 `test/dsl/test_emit_c.py`，使 `target="npu_demo"` 下 `ctx.thread_id()/ctx.thread_num()`、`ctx.get_dynamic_memory<T>(MemorySpace::TSM/TLM)`、`view/slice/deslice/add` 的节点级发射与已合并 `N2` spec 一致；不得回退到 `source.view<T>()`、`load/store`、`launch/barrier` 或 `arch.launch_kernel`。
改动：
- 核对任务前置：
  - `worktree=/home/lfr/kernelcode_generate/wt-20260402-npu-impl-e2` 可访问。
  - 已用主仓任务表 `/home/lfr/kernelcode_generate/TODO.md` 执行 `-status -doing`，确认当前我名下仅 `T-20260402-0a69ba66` 在进行中。
  - 已按要求向管理员同步“已开工、当前无其他进行中任务、无阻塞”。
- 只读核对当前基线：
  - `wt-20260402-npu-impl-e2/spec/dsl/emit_c.md` 已有 `npu_demo` 节点级合同，但 `kernel_gen/dsl/emit_c.py` 与 `test/dsl/test_emit_c.py` 尚无对应实现与测试。
  - 当前 `emit_c.py` 只覆盖 CPU 的 `dma.alloc/view/slice/deslice`、`nn.img2col2d` 路径，`target=npu_demo` 仍会走 `unsupported op`。
- 更新 `wt-20260402-npu-impl-e2/kernel_gen/dsl/emit_c.py`：
  - 新增 `ArchGetThreadIdOp`、`ArchGetThreadNumOp`、`ArchGetDynamicMemoryOp`、`NnAddOp` 导入。
  - 新增 `target=npu_demo` 的最小发射分支：
    - `arch.get_thread_id` -> `long long <name> = ctx.thread_id();`
    - `arch.get_thread_num` -> `long long <name> = ctx.thread_num();`
    - `arch.get_dynamic_memory` -> `Memory<T> <name> = ctx.get_dynamic_memory<T>(MemorySpace::TSM/TLM);`
    - `dma.view` -> `auto <name> = view(source, offset, size, stride);`
    - `dma.slice` -> `slice(target, source, offset, size, stride);`
    - `dma.deslice` -> `deslice(source, target, offset, size, stride);`
    - `nn.add` -> `add(lhs, rhs, out);`
  - 在 `npu_demo` 分支下明确：
    - 只接受 `TSM/TLM` dynamic memory；
    - `view/slice/deslice` 当前只支持 1-D `offset/size/stride`；
    - `nn.add` 仅支持 `memory + memory` 且结果必须预绑定为现有目标 memory 名称；
    - 不回退到 `.view<T>()`、`load/store`、`launch/barrier` 或 `arch.launch_kernel`。
  - 保留现有通用常量节点行为，不让 `arith.constant` 在 `npu_demo` 下误报 unsupported。
- 更新 `wt-20260402-npu-impl-e2/test/dsl/test_emit_c.py`：
  - 新增 `npu_demo` context helper。
  - 新增 `test_emit_c_lowers_npu_demo_kernel_context_queries`，锁定 `ctx.thread_id()` / `ctx.thread_num()`。
  - 新增 `test_emit_c_lowers_npu_demo_dynamic_memory_access`，锁定 `ctx.get_dynamic_memory<float>(MemorySpace::TSM/TLM)`。
  - 新增 `test_emit_c_lowers_npu_demo_slice_deslice_add_pipeline`，锁定 `view + slice + add + deslice` 的节点级文本，并断言输出中不出现 `.view<`、`load<`、`store<`、`launch`、`barrier`、`arch.launch_kernel`。
- 验证：
  - `pytest -q wt-20260402-npu-impl-e2/test/dsl/test_emit_c.py`（exit 0，`15 passed in 0.25s`）
  - `git -C wt-20260402-npu-impl-e2 diff --check -- kernel_gen/dsl/emit_c.py test/dsl/test_emit_c.py`（exit 0）
结论：
- `完成`。
- 影响范围仅：
  - `wt-20260402-npu-impl-e2/kernel_gen/dsl/emit_c.py`
  - `wt-20260402-npu-impl-e2/test/dsl/test_emit_c.py`
  - 当前记录文件
- 测试情况：已运行 `test/dsl/test_emit_c.py` 全量用例，`15 passed`。
- 阻塞点：无。
- 下一步建议：新建唯一后续审查任务，在同一 `worktree` 中只读复核 `kernel_gen/dsl/emit_c.py` 与 `test/dsl/test_emit_c.py` 是否与已合并 `N2` spec 完全一致，重点核对 `npu_demo` 的 `KernelContext` 查询、`TSM/TLM` dynamic memory、`view/slice/deslice/add` 以及禁止回退项。

时间：2026-04-02 06:11:41 +0800
任务：T-20260402-313e5d84
任务目标：在 `wt-20260402-npu-impl-e2` 中只读复核 `kernel_gen/dsl/emit_c.py` 与 `test/dsl/test_emit_c.py` 是否与已合并 `N2` spec 一致。
改动：
- 静态对照 `wt-20260402-npu-impl-e2/spec/dsl/emit_c.md`、`ARCHITECTURE/plan/npu_demo_backend_plan.md`、`wt-20260402-npu-impl-e2/kernel_gen/dsl/emit_c.py`、`wt-20260402-npu-impl-e2/test/dsl/test_emit_c.py`。
- 复核实现：
  - `kernel_gen/dsl/emit_c.py` 第 767-944 行已为 `target=npu_demo` 单独收口 `dma.view`、`arch.get_thread_id`、`arch.get_thread_num`、`arch.get_dynamic_memory`、`dma.slice`、`dma.deslice`、`nn.add` 的节点级 helper 发射。
  - `thread_id/thread_num` 精确生成为 `ctx.thread_id()` / `ctx.thread_num()`；`dynamic memory` 精确生成为 `ctx.get_dynamic_memory<T>(MemorySpace::TSM/TLM)`，并显式拒绝 `TSM/TLM` 之外的 space。
  - `view/slice/deslice/add` 分别精确生成为 `view(source, offset, size, stride)`、`slice(target, source, offset, size, stride)`、`deslice(source, target, offset, size, stride)`、`add(lhs, rhs, out)`；未发现 `.view<T>()`、`load/store`、`launch/barrier` 或 `arch.launch_kernel` 回退分支。
- 复核测试：
  - `test/dsl/test_emit_c.py` 第 683-801 行已覆盖 `KernelContext` 查询、`TSM/TLM` dynamic memory、`view + slice + add + deslice` 三条 `N2` 验收路径，并对 `.view<`、`load<`、`store<`、`launch`、`barrier`、`arch.launch_kernel` 进行了负向断言。
- 验证：
  - `pytest -q test/dsl/test_emit_c.py -k 'npu_demo'`（exit 0，`3 passed, 12 deselected in 0.23s`）
  - `git diff -- kernel_gen/dsl/emit_c.py test/dsl/test_emit_c.py`
  - `rg -n "\\.view<|load<|store<|launch\\(|arch\\.launch_kernel|barrier|slice\\(source|target = slice|malloc\\(" kernel_gen/dsl/emit_c.py test/dsl/test_emit_c.py`
结论：
- `通过`。
- 漏洞排查结果：
  - 未发现 `source.view<T>()` / `load/store` 旁路。
  - 未发现 `launch` / `barrier` / `arch.launch_kernel` 混入节点级成功路径。
  - 未发现把 `slice` 降级为表达式式返回值、或把 `deslice` 降级为 CPU copy loop 的实现。
- 下一步建议：进入合并阶段，仅合并 `kernel_gen/dsl/emit_c.py`、`test/dsl/test_emit_c.py` 与本链路记录文件。

时间：2026-04-02 06:26:42 +0800
任务：T-20260402-02078676
任务目标：在 `wt-20260402-npu-impl-e2` 按最小范围合入 `kernel_gen/dsl/emit_c.py`、`test/dsl/test_emit_c.py` 与对应任务记录；不改 `gen_kernel/spec/实现/测试` 之外的范围；合并后完成 cleanup 与状态同步。
改动：
- 合并范围确认：
  - `git -C wt-20260402-npu-impl-e2 status --short` 仅显示 `kernel_gen/dsl/emit_c.py` 与 `test/dsl/test_emit_c.py` 为已修改；未发现超出授权范围的其他未提交改动。
  - 链路记录文件沿主仓路径 `agents/codex-multi-agents/log/task_records/2026/14/20260402-npu-impl-e2.md` 维护，本次合并同步带上该记录文件。
- 合入主仓内容：
  - 在主仓现有 `cpu/conv/nn.add` 路径之上，最小并入 `npu_demo` 节点级发射：
    - `arch.get_thread_id` -> `ctx.thread_id()`
    - `arch.get_thread_num` -> `ctx.thread_num()`
    - `arch.get_dynamic_memory` -> `ctx.get_dynamic_memory<T>(MemorySpace::TSM/TLM)`
    - `dma.view` -> `view(source, offset, size, stride)`
    - `dma.slice` -> `slice(target, source, offset, size, stride)`
    - `dma.deslice` -> `deslice(source, target, offset, size, stride)`
    - `nn.add` -> `add(lhs, rhs, out)`
  - 由于授权 `worktree` 基线早于主仓已合入的 `cpu nn.add` 链路，本次未做整文件覆盖，而是在主仓当前版本上手工并入 `npu_demo` 规则，保留了既有 CPU 路径与相关测试，不回退主线内容。
  - 在 `test/dsl/test_emit_c.py` 追加 `_npu_ctx()` 与 `EC-017/018/019` 三组 `npu_demo` 回归测试，同时保留主仓已有的 `EC-008A/008B` 等 CPU 用例。
- 验证：
  - `pytest -q test/dsl/test_emit_c.py`（exit 0，`17 passed in 0.31s`）
  - `pytest -q test/dsl/test_emit_c.py -k 'npu_demo'`（exit 0，`3 passed, 14 deselected in 0.30s`）
  - `git diff --check -- kernel_gen/dsl/emit_c.py test/dsl/test_emit_c.py`（exit 0）
- cleanup：
  - `git worktree remove --force wt-20260402-npu-impl-e2`（exit 0）
  - `git branch -D wt-20260402-npu-impl-e2`（exit 0）
  - `git worktree list --porcelain` 复核后，授权 `worktree` 已移除，且未波及其他活跃 worktree。
结论：
- 已完成合并收口；本次仅合入 `kernel_gen/dsl/emit_c.py`、`test/dsl/test_emit_c.py` 与当前链路记录文件，未扩展到其他实现、测试或 spec 文件。
- 测试情况：`test/dsl/test_emit_c.py` 全量通过，结果 `17 passed`；`npu_demo` 定点用例 `3 passed, 14 deselected`。
- 阻塞点：无。
- 下一步建议：该链路已收口，无需继续派生后续任务；若后续要推进 `npu_demo` 的更高层实现或 expectation 闭环，请由管理员单独分发新任务。
