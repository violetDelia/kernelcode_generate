时间：2026-04-02 04:36:12 +0800
任务：T-20260402-1b186e9c
任务目标：按 `ARCHITECTURE/plan/npu_demo_backend_plan.md` 的 `N3`，仅修改 `spec/dsl/gen_kernel.md`，冻结 `target="npu_demo"` 的函数级 body-level kernel 骨架。
改动：
- 核对任务前置：
  - `worktree=/home/lfr/kernelcode_generate/wt-20260402-npu-n3` 可访问。
  - 当前无其他由睡觉小分队进行中的任务，已按要求向管理员同步开工状态。
- 更新 `spec/dsl/gen_kernel.md`：
  - 在 `目标` 与 `限制与边界` 中新增 `target="npu_demo"` 的函数级 body-level kernel 骨架合同，明确只冻结 body-level kernel，不定义 host wrapper、`launch`、`arch.launch_kernel`、`barrier` 或其他调度骨架。
  - 在 `gen_signature(...)` 注意事项中写清 `npu_demo` 的签名形态：首参必须为 `npu_demo::KernelContext& ctx`，随后为只读 `Memory` 输入和显式 `Memory` 输出参数，并补充接近 `void demo_kernel(npu_demo::KernelContext& ctx, const Memory<float>& source, Memory<float>& out)` 的目标源码示例。
  - 在 `gen_body(...)` 注意事项中冻结 body-level 骨架顺序：`ctx.thread_id()` / `ctx.thread_num()` -> `ctx.get_dynamic_memory<float>(MemorySpace::TSM/TLM)` -> `view(...)` -> `slice(...)` -> `add(...)` -> `deslice(...)`。
  - 明确函数骨架不得回退到 `.view<T>()`、`load<...>`、`store<...>`、表达式式 `slice(source, ...)`、`launch` 或 `barrier`。
  - 在测试目标与功能清单中补入 `N3` 的两个验收项，并标注为下游待补测试映射：`test_gen_kernel_emits_npu_demo_body_level_kernel` 与 `test_gen_kernel_emits_npu_demo_memory_pipeline`。
- 自检：
  - `rg -n "npu_demo::KernelContext& ctx|thread_id\\(|thread_num\\(|get_dynamic_memory|view\\(|slice\\(|deslice\\(|add\\(" spec/dsl/gen_kernel.md`（exit 0）
  - `git diff --check -- spec/dsl/gen_kernel.md`（exit 0）
结论：
- 已完成本任务要求，变更范围仅 `wt-20260402-npu-n3/spec/dsl/gen_kernel.md` 与本记录文件。
- 测试情况：本任务为 spec 阶段，未运行 `pytest`；已完成关键词校验与 diff 格式校验。
- 下一步建议：创建唯一后续实现任务，在同一 `worktree` 中对齐 `kernel_gen/dsl/gen_kernel.py` 与 `test/dsl/test_gen_kernel.py`，按当前 `N3` spec 实现 `npu_demo` 的函数级 body-level kernel 骨架。
时间：2026-04-02 04:46:29 +0800
任务：T-20260402-e5ec24f1
任务目标：审查 `spec/dsl/gen_kernel.md` 是否与 `ARCHITECTURE/plan/npu_demo_backend_plan.md` 的 `N3` 目标、边界、注意事项、验证命令、验收标准一致；重点核对 `target="npu_demo"` 的函数级 body-level kernel 骨架是否已写清 `npu_demo::KernelContext& ctx`、`thread_id/thread_num`、`TSM/TLM dynamic memory`、`view/slice/deslice/add` 的顺序，并确认未出现 `launch/barrier/.view/load/store`。
改动：
- 只读审查范围：
  - `spec/dsl/gen_kernel.md`
  - `ARCHITECTURE/plan/npu_demo_backend_plan.md`
  - `test/dsl/test_gen_kernel.py`
  - 本链路记录文件
- 复核命令：
  - `rg -n "npu_demo::KernelContext& ctx|thread_id\\(|thread_num\\(|get_dynamic_memory|view\\(|slice\\(|deslice\\(|add\\(" spec/dsl/gen_kernel.md`（exit 0）
  - `rg -n "test_gen_kernel_emits_npu_demo_body_level_kernel|test_gen_kernel_emits_npu_demo_memory_pipeline" test/dsl/test_gen_kernel.py`（exit 1）
  - `pytest -q test/dsl/test_gen_kernel.py -k 'npu_demo_body_level_kernel or npu_demo_memory_pipeline'`（exit 5，`12 deselected in 0.43s`）
  - `git diff --check -- spec/dsl/gen_kernel.md`（exit 0）
- 问题列表：
  - 本次审查未发现必须修改项。
- 漏洞排查结果：
  - 输入校验绕过：spec 未把 `npu_demo` 骨架写成“结构由实现决定”，没有放宽输入域或调度入口。
  - 类型/形状绕过：未回退到 `kernel dialect` 中转，也未放宽到 `.view<`、`load<`、`store<` 等旁路结构。
  - 边界越界：`N3` 边界仍限定在 `spec/dsl/gen_kernel.md`，未扩散到 `emit_c`、实现、测试。
  - 错误处理缺失：不支持路径仍保持明确报错口径，未引入 silent fallback。
  - 状态污染：spec-only 审查，未见状态污染问题。
  - 资源释放问题：spec-only 审查，未涉及资源生命周期变更。
- 改进建议：
  - 未发现额外改进点。
- 残余风险：
  - `test_gen_kernel_emits_npu_demo_body_level_kernel` 与 `test_gen_kernel_emits_npu_demo_memory_pipeline` 目前尚未落地到 `test/dsl/test_gen_kernel.py`；但当前 spec 已在 `GK-013/014` 中把它们明确标注为下游待补测试映射，没有误写成现有测试映射。
结论：
- `通过`。
- 审查结论：
  - `target="npu_demo"` 的函数级 body-level kernel 骨架已写清 `npu_demo::KernelContext& ctx`、`ctx.thread_id()` / `ctx.thread_num()`、`ctx.get_dynamic_memory<float>(MemorySpace::TSM/TLM)`、以及 `view -> slice -> add -> deslice` 的固定顺序。
  - 未出现 `launch`、`barrier`、`.view<`、`load<`、`store<` 等禁止项，也未回退为 host wrapper 或其他调度骨架。
  - `N3` 计划中的两个验收测试名已与 spec 对齐，并明确降级为下游待补测试映射。
- 下一步建议：新建唯一后续任务，进入合并阶段，仅合入 `wt-20260402-npu-n3/spec/dsl/gen_kernel.md` 与当前链路记录文件。
