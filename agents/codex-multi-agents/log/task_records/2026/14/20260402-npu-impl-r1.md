时间：2026-04-02 05:43:28 +0800
任务：T-20260402-c9d9220b
任务目标：在 `wt-20260402-npu-impl-r1` 中对齐 `kernel_gen/target/registry.py` 与 `test/target/test_target_registry.py`，使 `npu_demo` 固定模板、硬件值与不支持项查询行为按已合并 `N1` spec 生效；不改 `emit_c/gen_kernel/spec`。
改动：
- 核对任务前置：
  - `worktree=/home/lfr/kernelcode_generate/wt-20260402-npu-impl-r1` 可访问。
  - 已用主仓 `TODO.md` 执行 `-status -doing`，确认当前我名下仅 `T-20260402-c9d9220b` 在进行中。
  - 当前 `worktree` 内记录文件不存在，按规范在 `wt-20260402-npu-impl-r1/agents/codex-multi-agents/log/task_records/2026/14/20260402-npu-impl-r1.md` 新建。
- 对齐 `N1` 规范与实现：
  - 读取已合并的 `spec/target/registry.md` 与 `20260402-npu-demo-n1.md`，确认 `npu_demo` 必须作为固定内置模板提供，白名单支持 `arch.get_block_id/get_block_num/get_thread_id/get_thread_num/get_subthread_id/get_subthread_num/get_dynamic_memory`，固定未启用 `arch.launch_kernel`，硬件值固定为 `block_num=6`、`thread_num=8`、`subthread_num=1`、`sm/lm=0`、`tsm=24576`、`tlm=2048`。
  - 更新 `kernel_gen/target/registry.py`：
    - 新增 `_DEFAULT_NPU_DEMO_SUPPORTED_OPS`、`_DEFAULT_NPU_DEMO_UNSUPPORTED_OPS`、`_DEFAULT_NPU_DEMO_HARDWARE` 三组固定模板常量。
    - 新增 `_ensure_npu_demo_target()` 内置注册函数，并在模块初始化阶段与 `_ensure_cpu_target()` 一起执行，保证不依赖目录扫描即可直接查询 `npu_demo`。
  - 保持现有查询行为不扩散到其他模块：`is_arch_op_supported(...)` 继续按显式白名单/黑名单判断，因此 `arch.launch_kernel`、`launch`、`barrier`、未知能力对 `npu_demo` 都固定收敛为未启用。
- 补齐测试 `test/target/test_target_registry.py`：
  - 新增 `test_target_registry_npu_demo_template`，验证 `npu_demo` 固定能力矩阵和全部硬件值。
  - 新增 `test_target_registry_npu_demo_rejects_unsupported_ops`，验证 `arch.launch_kernel`、未知 `arch.*`、以及接口域外 `launch/barrier` 查询都返回未启用。
- 验证：
  - `pytest -q test/target/test_target_registry.py`（exit 0，`17 passed in 0.24s`）
  - `git diff --check -- kernel_gen/target/registry.py test/target/test_target_registry.py`（exit 0）
结论：
- 已完成本任务要求，影响范围仅 `wt-20260402-npu-impl-r1/kernel_gen/target/registry.py`、`wt-20260402-npu-impl-r1/test/target/test_target_registry.py` 与本记录文件。
- 测试情况：`pytest -q test/target/test_target_registry.py` 通过，`17 passed in 0.24s`。
- 阻塞点：无。
- 下一步建议：新建唯一后续审查任务，在同一 `worktree` 中只读复核 `kernel_gen/target/registry.py`、`test/target/test_target_registry.py` 与已合并 `spec/target/registry.md` 是否完全一致，重点检查 `npu_demo` 内置模板入口、固定硬件值、`arch.launch_kernel` 未启用与 `launch/barrier` 收敛结果。
时间：2026-04-02 05:49:29 +0800
任务：T-20260402-372362d9
任务目标：在 `wt-20260402-npu-impl-r1` 中只读复核 `kernel_gen/target/registry.py` 与 `test/target/test_target_registry.py` 是否与已合并 `N1` spec 完全一致，重点检查 `npu_demo` 固定模板、硬件值与未启用项查询行为；不改 `spec/emit_c/gen_kernel`。
改动：
- 只读审查范围：
  - `spec/target/registry.md`
  - `kernel_gen/target/registry.py`
  - `test/target/test_target_registry.py`
  - 当前链路记录文件
- 复核命令：
  - `pytest -q test/target/test_target_registry.py`（exit 0，`17 passed in 0.20s`）
  - `git diff --check -- kernel_gen/target/registry.py test/target/test_target_registry.py`（exit 0）
- 问题列表：
  - 本次审查未发现必须修改项。
- 漏洞排查结果：
  - 输入校验绕过：`_ensure_npu_demo_target()` 走固定内置模板注册，未暴露额外外部输入绕过路径。
  - 类型/形状绕过：`is_arch_op_supported(...)` 在白名单 target 下按显式 `supported_ops` 判定，`arch.launch_kernel`、未知 `arch.*`、`launch`、`barrier` 都固定收敛为未启用。
  - 边界越界：实现仍把 `npu_demo` 限定为固定模板，不依赖 `json/txt` 文件注册，不会扩散到 `emit_c/gen_kernel`。
  - 错误处理缺失：未注册 target、非法目录、非法配置项等仍保持明确报错。
  - 状态污染：固定模板在模块初始化阶段幂等注册，未见重复注册导致的状态污染。
  - 资源释放问题：纯查询/注册逻辑，不涉及资源生命周期问题。
- 改进建议：
  - 未发现额外改进点。
结论：
- `通过`。
- 审查结论：
  - `npu_demo` 固定模板已通过 `_DEFAULT_NPU_DEMO_SUPPORTED_OPS`、`_DEFAULT_NPU_DEMO_UNSUPPORTED_OPS`、`_DEFAULT_NPU_DEMO_HARDWARE` 与 `_ensure_npu_demo_target()` 固化，符合已合并 `N1` spec。
  - `block_num=6`、`thread_num=8`、`subthread_num=1`、`sm/lm=0`、`tsm=24576`、`tlm=2048` 的硬件值与 spec 一致。
  - `arch.launch_kernel`、未知 `arch.*`、以及接口域外 `launch/barrier` 查询都固定返回未启用，符合 spec。
- 下一步建议：新建唯一后续任务，进入合并阶段，仅合入 `wt-20260402-npu-impl-r1/kernel_gen/target/registry.py`、`wt-20260402-npu-impl-r1/test/target/test_target_registry.py` 与当前链路记录文件。
时间：2026-04-02 05:53:22 +0800
任务：T-20260402-d75c757d
任务目标：将 `wt-20260402-npu-impl-r1` 中已通过审查的 `npu_demo target registry` 实现成果按最小范围合入主分支，仅包含 `kernel_gen/target/registry.py`、`test/target/test_target_registry.py` 与同链路记录文件；完成单次同步、cleanup 与状态封板。
改动：
- 核对合并边界：`TODO.md` 中当前 `worktree=wt-20260402-npu-impl-r1` 仅存在本任务 `T-20260402-d75c757d`；`git -C wt-20260402-npu-impl-r1 status --short` 仅见 `kernel_gen/target/registry.py`、`test/target/test_target_registry.py` 与同链路记录文件变更，未发现范围外业务文件。
- 复核主分支差异：`git diff --name-only 530ea2d..HEAD -- kernel_gen/target/registry.py test/target/test_target_registry.py` 无输出，确认自 `wt-20260402-npu-impl-r1` 基线以来主仓未再修改这两个文件，因此本次无需人工冲突合并。
- 将 `wt-20260402-npu-impl-r1/kernel_gen/target/registry.py`、`wt-20260402-npu-impl-r1/test/target/test_target_registry.py` 与同链路记录同步到主分支工作目录，复核 `git diff --check -- kernel_gen/target/registry.py test/target/test_target_registry.py agents/codex-multi-agents/log/task_records/2026/14/20260402-npu-impl-r1.md` 通过。
- 在主分支生成合并提交 `90906fe`（`T-20260402-d75c757d-merge-npu-impl-r1`），提交内容仅包含 `kernel_gen/target/registry.py`、`test/target/test_target_registry.py` 与 `agents/codex-multi-agents/log/task_records/2026/14/20260402-npu-impl-r1.md`。
- 执行 cleanup：`git worktree remove --force wt-20260402-npu-impl-r1` 与 `git branch -D wt-20260402-npu-impl-r1`（均 exit 0）；清理后 `git worktree list --porcelain` 不再包含 `wt-20260402-npu-impl-r1`，其余现存 worktree 保持原样未触碰。
- 未新增本轮测试；本次合并直接引用链路内已有验证 `pytest -q test/target/test_target_registry.py`（`17 passed in 0.20s`）与静态对照结论。
结论：
- 完成。`npu_demo target registry` 实现成果已按限定范围合入主分支，对应 worktree/branch 已清理，无范围外文件混入。
- 本任务未扩到 `emit_c/gen_kernel/spec`；下一步建议由管理员按计划单独创建后续任务。
